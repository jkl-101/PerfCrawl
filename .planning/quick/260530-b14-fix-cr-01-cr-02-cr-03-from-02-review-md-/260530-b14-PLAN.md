---
quick_id: 260530-b14
phase: 02-single-page-measurement-slice
type: quick
wave: 1
depends_on: []
files_modified:
  - lighthouse-worker/run.mjs
  - src/perfcrawl/orchestrator.py
  - tests/test_worker.py
  - tests/test_orchestrator.py
autonomous: true
requirements:
  - CR-01
  - CR-02
  - CR-03
  - VERIF-REGRESSION-WORKER-PIPE

must_haves:
  truths:
    - "A real Node subprocess can stream a >1 MB JSON payload to Python's subprocess.run without truncation (regression test goes RED before CR-01, GREEN after)"
    - "uv run perfcrawl measure https://example.com --samples 1 succeeds end-to-end (exit 0, on-disk artifacts created) instead of failing with 'all 1 samples failed'"
    - "After measure_url completes (success or MeasurementError), no <defunct> Chromium remains in the process table — every chrome.kill() is paired with chrome.wait(timeout=5)"
    - "After _launch_chrome_with_cdp_port raises MeasurementError on the DevToolsActivePort timeout path, the per-run user_data_dir tempdir has been shutil.rmtree'd (no /tmp leak)"
    - "uv run pytest -m e2e tests/test_e2e.py -x goes green against https://example.com after all three fixes land"
  artifacts:
    - path: "lighthouse-worker/run.mjs"
      provides: "CR-01: callback-form process.stdout.write(payload, () => process.exit(0)) drains stdout before exit"
      contains: "process.stdout.write(payload, (err)"
    - path: "src/perfcrawl/orchestrator.py"
      provides: "CR-02: chrome.wait(timeout=5) (catching TimeoutExpired) after every chrome.kill(); CR-03: shutil.rmtree(user_data_dir, ignore_errors=True) before raise on DevToolsActivePort timeout"
      contains: "chrome.wait(timeout=5)"
    - path: "tests/test_worker.py"
      provides: "Regression test pumping a >1 MB synthetic JSON payload through a real Node subprocess (no subprocess.run mock) and asserting Python parses the full envelope"
      contains: "test_worker_drains_large_stdout_payload"
    - path: "tests/test_orchestrator.py"
      provides: "Regression assertions: chrome_proc.waited is True on both success and failure; not user_data_dir.exists() after DevToolsActivePort timeout"
      contains: "chrome_proc.waited"
  key_links:
    - from: "tests/test_worker.py::test_worker_drains_large_stdout_payload"
      to: "lighthouse-worker/run.mjs (callback-form drain pattern)"
      via: "real subprocess.run([node, shim.mjs]) with a 1.5 MB payload"
      pattern: "len\\(proc\\.stdout\\) > 1_000_000"
    - from: "tests/test_orchestrator.py::test_chrome_killed_on_{success,failure}"
      to: "orchestrator.measure_url's finally block"
      via: "_FakeChromeProc.waited flag set by .wait()"
      pattern: "chrome_proc\\.waited is True"
    - from: "tests/test_orchestrator.py::test_devtools_port_timeout_raises"
      to: "_launch_chrome_with_cdp_port cleanup-on-timeout path"
      via: "tmp_path assertions after the launcher raises"
      pattern: "not user_data_dir\\.exists\\(\\)"
---

<objective>
Close the three Critical findings (CR-01, CR-02, CR-03) from `02-REVIEW.md` that
`02-VERIFICATION.md` (2026-05-29T18:30Z, status `gaps_found`) confirmed as still
open, and add the worker-pipe regression test the verifier explicitly demanded.

This is a quick-task closeout of a verification gap on the active Phase 02 branch
(`gsd/phase-02-single-page-measurement-slice`). It does NOT introduce new
behavior — every patch is already specified verbatim in `02-REVIEW.md` (§ CR-01,
§ CR-02, § CR-03). The planner's job is to wrap each patch in an atomic
test-first → fix-after commit pair so the regression is locked in by a RED→GREEN
test boundary.

Purpose: re-establish the phase-goal invariant that `uv run perfcrawl measure
<url>` works end-to-end against a real URL. Today the command exits 2 with
"all 1 samples failed" because real Lighthouse payloads (200KB-2MB) get
truncated at the ~64KB kernel pipe buffer between Node and Python — CR-01 is
the load-bearing fix; CR-02 and CR-03 are correctness/hygiene siblings (zombie
processes and tempdir leaks) that ship in the same closeout.

Output: 4 task-level commits on the current branch following Phase 02's existing
`test(02): RED — …` → `fix(02): … — …` cadence (see commits `b4828a8` /
`cb0439f` / `f2ffa11` for the established pattern). The final commit is the
load-bearing CR-01 fix; running `uv run pytest tests/test_worker.py
tests/test_orchestrator.py` after each commit should show the corresponding
RED test going from failing to passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-single-page-measurement-slice/02-VERIFICATION.md
@.planning/phases/02-single-page-measurement-slice/02-REVIEW.md
@CLAUDE.md

# Source files touched by each task
@lighthouse-worker/run.mjs
@src/perfcrawl/orchestrator.py
@src/perfcrawl/lighthouse_worker.py
@tests/test_worker.py
@tests/test_orchestrator.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test(02): RED — CR-02 chrome_proc.waited asserted on both success and failure paths</name>
  <files>tests/test_orchestrator.py</files>
  <behavior>
    Two assertions added/strengthened:
    - `test_chrome_killed_on_success`: after `measure_url(...)` returns
      normally, `chrome_proc.waited is True` (not just `chrome_proc.killed is
      True`).
    - `test_chrome_killed_on_failure`: after `measure_url(...)` raises
      `MeasurementError` (samples all fail path), `chrome_proc.waited is
      True`.
    - `test_tempdir_cleaned_on_failure`: also pin `chrome_proc.waited is True`
      so the finally-block reap covers the rmtree-also-runs scenario.
    Both rely on `_FakeChromeProc` exposing `waited: bool` flipped to True by
    a `.wait(timeout=…)` call (already in conftest-equivalent helper at
    tests/test_orchestrator.py top — verify it's there; if not, add `waited`
    + `wait(timeout=None)` method to the existing fake class).
  </behavior>
  <action>
    Open `tests/test_orchestrator.py` and locate the `_FakeChromeProc` helper
    class (currently tracks `.killed`). If it doesn't already, extend it with
    a `waited: bool` attribute and a `wait(self, timeout=None) -> int` method
    that sets `self.waited = True` and returns 0. Then in the three named
    tests (`test_chrome_killed_on_success`, `test_chrome_killed_on_failure`,
    `test_tempdir_cleaned_on_failure`), add `assert chrome_proc.waited is
    True` immediately after each existing `assert chrome_proc.killed is True`.
    Comment the new line as the CR-02 invariant pin (matching the existing
    inline `# CR-02: …` comment style in the file).

    DO NOT modify `orchestrator.py` in this task. The test must be RED before
    the fix lands so the RED→GREEN cycle is genuine. Run the test suite to
    confirm RED:
      `uv run pytest tests/test_orchestrator.py::test_chrome_killed_on_success tests/test_orchestrator.py::test_chrome_killed_on_failure tests/test_orchestrator.py::test_tempdir_cleaned_on_failure -x`
    Expected: AssertionError on `chrome_proc.waited is True` (still False).

    Commit message: `test(02): RED — CR-02 chrome.wait() must reap after chrome.kill()`
    Commit author trailer: `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator.py::test_chrome_killed_on_success tests/test_orchestrator.py::test_chrome_killed_on_failure tests/test_orchestrator.py::test_tempdir_cleaned_on_failure -x 2>&1 | grep -E "(AssertionError|chrome_proc\.waited)" | head -5</automated>
  </verify>
  <done>
    Three named tests now assert `chrome_proc.waited is True`. Running the
    three tests against the pre-fix orchestrator fails with `AssertionError:
    assert False is True` on the `waited` line. Commit landed with subject
    starting `test(02): RED — CR-02`. The `_FakeChromeProc` helper has
    `waited` + `wait(timeout=…)` available so the next task's fix can flip
    it green without further test-file changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: fix(02): CR-02 — chrome.wait(timeout=5) after every chrome.kill()</name>
  <files>src/perfcrawl/orchestrator.py</files>
  <action>
    Apply the CR-02 patch exactly as specified in `02-REVIEW.md` § CR-02 (and
    cross-referenced by `02-VERIFICATION.md` gap #2, artifact line 250 +
    launcher line 120).

    Two call sites:

    1. `_launch_chrome_with_cdp_port` DevToolsActivePort-timeout path (around
       the existing `proc.kill()` before the `raise MeasurementError(...)`):
         after `proc.kill()`, add `try: proc.wait(timeout=5)` /
         `except subprocess.TimeoutExpired: pass` BEFORE the rmtree + raise.
       Inline comment: `# CR-02: reap so it never becomes a <defunct> zombie`.

    2. `measure_url`'s outer `finally` block (around the existing
       `chrome.kill()`):
         after `chrome.kill()`, add `try: chrome.wait(timeout=5)` /
         `except subprocess.TimeoutExpired: pass`. Keep the `try/except
         Exception: pass` outer guard so a `.wait()` raising something
         exotic still does not mask the original measurement exception.

    Use `subprocess.TimeoutExpired` (the existing `import subprocess` at the
    top of the file already covers it).

    Sanity-check: `grep -n 'chrome.kill\|proc.kill' src/perfcrawl/orchestrator.py`
    should show every `.kill()` call has a `.wait(timeout=5)` within the next
    ~5 lines.

    Commit message: `fix(02): CR-02 — chrome.wait(timeout=5) reaps Chrome after kill (no <defunct> zombie)`
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator.py::test_chrome_killed_on_success tests/test_orchestrator.py::test_chrome_killed_on_failure tests/test_orchestrator.py::test_tempdir_cleaned_on_failure -x</automated>
  </verify>
  <done>
    Both `chrome_proc.waited is True` assertions added in Task 1 now pass
    (test goes GREEN). Full default suite (`uv run pytest`) remains green
    (no other test regresses). The `grep` sanity check shows `kill()` and
    `wait(timeout=5)` paired at every call site in `orchestrator.py`.
    Commit landed with subject starting `fix(02): CR-02`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test(02): RED + fix(02): CR-03 — rmtree(user_data_dir) before raise on DevToolsActivePort timeout</name>
  <files>tests/test_orchestrator.py, src/perfcrawl/orchestrator.py</files>
  <behavior>
    Extend `test_devtools_port_timeout_raises` so it pins the CR-03
    invariant in addition to the existing CR-02 reap assertion:
    - After `_launch_chrome_with_cdp_port()` raises `MeasurementError`, the
      per-run `user_data_dir` (a tmp_path subdir created by the test) must
      no longer exist on disk: `assert not user_data_dir.exists()`.
    The test must fail on a pre-CR-03 launcher (the rmtree never runs on
    the timeout path) and pass after the fix.
  </behavior>
  <action>
    This task pairs a single RED test extension with the matching fix in one
    commit each (two commits, in this order). The fix is small enough that
    keeping it in the same task as the test extension is cleaner than
    splitting across two tasks — but the RED→GREEN boundary is still
    enforced via the two commit-and-verify steps below.

    Step 3a — RED commit (`test(02): RED — CR-03 …`):
    In `tests/test_orchestrator.py::test_devtools_port_timeout_raises`, add
    `assert not user_data_dir.exists()` immediately after the existing
    `assert fake_proc.killed is True` block (and after the CR-02 `.waited`
    assertion from Task 1, if it's present). Cite the inline comment
    `# CR-03: launcher cleans its own user_data_dir on the timeout-raise path`.
    Run the single test:
      `uv run pytest tests/test_orchestrator.py::test_devtools_port_timeout_raises -x`
    Confirm RED on the `not user_data_dir.exists()` line. Commit.

    Step 3b — GREEN commit (`fix(02): CR-03 …`):
    In `src/perfcrawl/orchestrator.py::_launch_chrome_with_cdp_port`, locate
    the DevToolsActivePort-timeout path — the block that runs after the
    polling loop exits without finding the file, currently:
      `proc.kill()`
      `try: proc.wait(timeout=5)` / `except TimeoutExpired: pass`   # added by Task 2
      `raise MeasurementError(f"Chrome did not write DevToolsActivePort within {DEVTOOLS_PORT_FILE_TIMEOUT_S}s")`
    Insert `shutil.rmtree(user_data_dir, ignore_errors=True)` between the
    `wait()`/except block and the `raise`. Inline comment: `# CR-03:
    self-contained cleanup — caller cannot rmtree because the
    chrome, port, user_data_dir = ...  assignment never completes`.

    `shutil` is already imported at the top of orchestrator.py (used by the
    outer finally's rmtree). No new import needed.

    Re-run the test:
      `uv run pytest tests/test_orchestrator.py::test_devtools_port_timeout_raises -x`
    Confirm GREEN. Commit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator.py::test_devtools_port_timeout_raises -x</automated>
  </verify>
  <done>
    `test_devtools_port_timeout_raises` asserts `not user_data_dir.exists()`
    AND that assertion passes after the GREEN commit. Two commits exist on
    HEAD: `test(02): RED — CR-03 …` then `fix(02): CR-03 …`. Full default
    suite remains green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: test(02): RED + fix(02): CR-01 — real-subprocess >1MB pipe regression test, then drain-before-exit in run.mjs</name>
  <files>tests/test_worker.py, lighthouse-worker/run.mjs</files>
  <behavior>
    This is the load-bearing fix and the verifier's explicit ask: a regression
    test that exercises the Node→Python stdout pipe with a real subprocess
    (NOT a `subprocess.run` mock) carrying a payload larger than every
    plausible kernel pipe buffer (Linux ~64KB, macOS ~16KB, FreeBSD ~16KB).

    Test contract (`test_worker_drains_large_stdout_payload`):
    - Skip cleanly when `shutil.which("node")` is None (CI without Node
      passes; verifier's machine has Node 22.x).
    - Write a tiny shim `shim.mjs` to a pytest `tmp_path` that does the same
      drain pattern run.mjs is about to adopt: build a JSON envelope whose
      total stringified size is >1,000,000 bytes (the verifier's threshold),
      then `process.stdout.write(payload, (err) => { process.exit(err ? 1 :
      0); })`. The shim is intentionally a literal copy of the drain pattern
      so the test fails RED if the pattern itself is wrong, not just if
      run.mjs hasn't adopted it.
    - Invoke `subprocess.run([node, str(shim)], capture_output=True,
      text=True, encoding="utf-8", timeout=10)` — REAL subprocess, NO
      monkeypatch.
    - Assert `proc.returncode == 0`.
    - Assert `len(proc.stdout) > 1_000_000` — this is the truncation
      failure mode under test; pre-fix this fails at 65536.
    - Assert `json.loads(proc.stdout)` round-trips the full envelope
      (`reportJson` is the full 1.5 MB string we put in).
  </behavior>
  <action>
    Two commits, in this order (RED → GREEN):

    Step 4a — RED commit (`test(02): RED — CR-01 …`):

    Add `test_worker_drains_large_stdout_payload(tmp_path: Path)` to
    `tests/test_worker.py` at the bottom of the file. Imports already
    present: `json`, `shutil`, `subprocess`, `pathlib.Path`, `pytest`.

    The shim it writes MUST use the same drain-before-exit pattern that the
    CR-01 fix is about to apply to `run.mjs`. Concretely:

      const big = "x".repeat(1_500_000);
      const payload = JSON.stringify({
        lhr: { lighthouseVersion: "13.3.0" },
        reportJson: big,
        reportHtml: big,
      });
      process.stdout.write(payload, (err) => {
        if (err) { process.stderr.write(String(err)); process.exit(1); }
        process.exit(0);
      });

    Use ``shim.write_text("…")`` from Python to create the file. Skip the
    test (not fail) via `pytest.skip("node binary not on PATH; CR-01
    regression requires Node runtime")` when `shutil.which("node") is None`.

    The test exists to pin CR-01 at the language/runtime level. The
    pre-fix shape (`process.stdout.write(payload); process.exit(0);`
    synchronously) would produce a stdout of exactly the pipe-buffer size
    (~65536 on Linux, ~16384 on macOS). Writing the shim with the
    callback-form pattern means the shim itself is GREEN — the RED
    state we're pinning is run.mjs's pre-fix behavior, which Step 4b
    fixes. So this test goes GREEN as soon as it's added (because the
    shim is correct) — but the verifier's stated intent is that this
    test EXISTS as a permanent regression guard against future
    accidental reintroduction of the synchronous-exit pattern in
    run.mjs.

    To make the RED→GREEN cycle genuine against `run.mjs` itself, also add
    a second test `test_run_mjs_uses_drain_callback_pattern` that
    statically asserts run.mjs contains the callback-form drain pattern.
    Read the file and assert via regex that `process.exit(0)` does NOT
    appear at the top level of the `try` block immediately after a
    `process.stdout.write(payload)` call. Cheaper grep guard:
      src = (REPO_ROOT / "lighthouse-worker" / "run.mjs").read_text()
      # Filter comments (CLAUDE.md memory: grep -c counts comments).
      non_comment = "\n".join(
          l for l in src.splitlines() if not l.lstrip().startswith("//")
      )
      assert "process.stdout.write(payload, (err)" in non_comment, (
          "CR-01 regression: run.mjs is using synchronous "
          "process.stdout.write + process.exit pattern; "
          "kernel pipe buffer will truncate real LH payloads"
      )
    This static check is what actually goes RED on pre-fix run.mjs and
    GREEN after Step 4b. The `_drains_large_stdout_payload` test is the
    runtime sibling that protects against the drain pattern itself being
    broken (independently of run.mjs).

    Run:
      `uv run pytest tests/test_worker.py::test_worker_drains_large_stdout_payload tests/test_worker.py::test_run_mjs_uses_drain_callback_pattern -x`
    Confirm RED on the static-check test (the pipe-round-trip test should
    pass already because its shim is self-correct). Commit.

    Step 4b — GREEN commit (`fix(02): CR-01 …`):

    Open `lighthouse-worker/run.mjs`. Locate the success-path block (the
    `try { … }` that calls `await lighthouse(values.url, flags, config)` and
    destructures `[reportJson, reportHtml] = result.report`).

    Replace the sequence:

      const payload = JSON.stringify({ lhr: result.lhr, reportJson, reportHtml });
      clearTimeout(watchdog);
      process.stdout.write(payload);
      process.exit(0);

    with the drain-before-exit pattern (per `02-REVIEW.md` § CR-01 and
    the IN-08 watchdog clarification — clearTimeout MUST move inside the
    callback so the watchdog still fires if the write hangs):

      const payload = JSON.stringify({ lhr: result.lhr, reportJson, reportHtml });
      // CR-01: drain stdout before exiting. Kernel pipe buffer is ~64KB
      // on Linux / ~16KB on macOS; real LH payloads are 200KB-2MB.
      // Callback form guarantees the kernel finishes the drain before
      // the worker process terminates.
      // IN-08: clearTimeout MUST be inside the callback so the watchdog's
      // lease covers the longest-running operation (the payload write).
      process.stdout.write(payload, (err) => {
        clearTimeout(watchdog);
        if (err) {
          process.stderr.write(
            `worker error: stdout write failed: ${err.message}\n`,
            () => process.exit(1),
          );
          return;
        }
        process.exit(0);
      });
      // Do NOT call process.exit synchronously after this point.

    Apply the same callback-form pattern to the catch-block stderr write
    (so a Lighthouse exception also drains stderr before exit-1):

      } catch (err) {
        clearTimeout(watchdog);
        process.stderr.write(
          `worker error: ${err.message}\n`,
          () => process.exit(1),
        );
      }

    And to the 55s watchdog timeout's stderr write at the top of the file
    (mirror pattern):

      const watchdog = setTimeout(() => {
        process.stderr.write(
          `worker error: self-terminated after ${WATCHDOG_MS}ms watchdog\n`,
          () => process.exit(1),
        );
      }, WATCHDOG_MS);

    Confirm `node lighthouse-worker/run.mjs` still parses (`node --check
    lighthouse-worker/run.mjs` if you want a syntax-only gate).

    Re-run:
      `uv run pytest tests/test_worker.py::test_worker_drains_large_stdout_payload tests/test_worker.py::test_run_mjs_uses_drain_callback_pattern -x`
    Both GREEN. Commit.

    Final end-to-end sanity (do NOT make this part of the commit, but DO
    run it to prove the phase goal is met):
      `cd lighthouse-worker && npm ci && cd ..`
      `uv run playwright install chromium`
      `uv run pytest -m e2e tests/test_e2e.py -x`
    Expect: exit 0, e2e test GREEN against https://example.com. If RED,
    investigate before declaring this task done — the verifier's gap #1
    is the canonical proof.
  </action>
  <verify>
    <automated>uv run pytest tests/test_worker.py::test_worker_drains_large_stdout_payload tests/test_worker.py::test_run_mjs_uses_drain_callback_pattern -x</automated>
  </verify>
  <done>
    Both new worker tests pass. `lighthouse-worker/run.mjs` uses the
    callback-form `process.stdout.write(payload, (err) => …)` pattern at
    every exit-after-write site (success path, catch-block stderr,
    watchdog stderr). Full default suite (`uv run pytest`) green.
    `uv run pytest -m e2e tests/test_e2e.py -x` GREEN against
    https://example.com (proving the phase-goal end-to-end the verifier
    flagged as failing). Two commits exist on HEAD: `test(02): RED — CR-01
    …` then `fix(02): CR-01 — …`.
  </done>
</task>

</tasks>

<verification>
After all four tasks complete, the executor MUST run the following gate
suite in order; ALL of them must pass before declaring the quick task closed:

1. `uv run pytest` — full default unit suite green (~188 tests). No
   regressions in any phase 02 module.

2. `cd lighthouse-worker && npm ci && cd ..` — Node side installed (no
   vulnerabilities, lockfile-clean).

3. `uv run playwright install chromium` — Chromium downloaded.

4. `uv run pytest -m e2e tests/test_e2e.py -x` — end-to-end smoke against
   https://example.com goes GREEN. **This is the load-bearing proof.** It
   exercises the real Node→Python pipe with a real Lighthouse payload, which
   is what `02-VERIFICATION.md` gap #1 said was failing pre-fix. If this is
   RED, CR-01 is not actually closed.

5. `uv run perfcrawl measure https://example.com --samples 1 --json --output-dir /tmp/perfcrawl-b14-smoke 2>&1 | head -5` —
   manual CLI smoke. Exit 0; `--json` emits a parseable JSON envelope on
   stdout; `/tmp/perfcrawl-b14-smoke/{result.json,result.csv,lighthouse/}`
   exist and have non-zero size.

6. `grep -nE 'kill\(' src/perfcrawl/orchestrator.py | grep -v '^[[:space:]]*#'` —
   every `.kill()` call site, manually verify a `.wait(timeout=…)` follows
   within ~5 lines. (Static sibling of the CR-02 unit pin.)

7. `grep -nE 'process\.stdout\.write|process\.stderr\.write' lighthouse-worker/run.mjs | grep -v '^[[:space:]]*//'` —
   every `process.stdout/stderr.write` call uses the callback form (a `,`
   followed by an arrow function appears before the closing `)` of the
   call). Manually verify no naked `process.stdout.write(payload);
   process.exit(N);` sequences remain. (Static sibling of the CR-01
   regression test.)
</verification>

<success_criteria>
- Six new commits exist on `gsd/phase-02-single-page-measurement-slice` in
  the documented order:
    1. `test(02): RED — CR-02 chrome.wait() must reap after chrome.kill()`
    2. `fix(02): CR-02 — chrome.wait(timeout=5) reaps Chrome after kill …`
    3. `test(02): RED — CR-03 launcher must rmtree user_data_dir on timeout`
    4. `fix(02): CR-03 — rmtree(user_data_dir, ignore_errors=True) before raise`
    5. `test(02): RED — CR-01 worker pipe round-trips >1MB payload via real subprocess`
    6. `fix(02): CR-01 — drain stdout via callback before process.exit(0)`

- All four `auto`-mode tasks above declare DONE per their `<done>` blocks.
- All seven verification gates pass.
- `02-VERIFICATION.md` gaps #1 and #2 are closed (verifier can re-run on the
  new HEAD and flip `status: gaps_found` → `verified`).
- Phase-goal invariant restored: `uv run perfcrawl measure <url>` produces
  on-disk artifacts in real execution against a real URL.
- No new TODOs / FIXMEs / XXX markers introduced in modified files.
</success_criteria>

<output>
Create `.planning/quick/260530-b14-fix-cr-01-cr-02-cr-03-from-02-review-md-/260530-b14-SUMMARY.md`
when done. The SUMMARY must enumerate:
  - The six commit SHAs (in order) and their subjects.
  - The result of each of the seven verification gates (PASS / FAIL / SKIP).
  - The output of `uv run perfcrawl measure https://example.com --samples 1`
    (stdout exit code + first 5 lines of on-disk `result.json`).
  - Any deviation from the planned commit order/structure, with rationale.

Note: SUMMARY lives in `.planning/quick/…` which is gitignored (per global
CLAUDE.md policy); do not branch/PR for it. The source-code commits (six
commits above) are on the active phase branch and will ship as part of
Phase 02's normal `/gsd-ship 2` flow.
</output>
