---
phase: 02-single-page-measurement-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lighthouse-worker/package.json
  - lighthouse-worker/package-lock.json
  - lighthouse-worker/run.mjs
  - lighthouse-worker/.gitignore
  - src/perfcrawl/constants.py
  - src/perfcrawl/slug.py
  - src/perfcrawl/normalizer.py
  - tests/conftest.py
  - tests/fixtures/lighthouse/studyhalo-home-200.json
  - tests/fixtures/lighthouse/studyhalo-404.json
  - tests/fixtures/lighthouse/version-drift-14.json
  - tests/test_slug.py
  - tests/test_normalizer.py
autonomous: true
requirements:
  - METRIC-01
  - METRIC-02
  - METRIC-03
  - METRIC-04
  - METRIC-05
  - RUN-01
  - RUN-02
user_setup:
  - service: node
    why: "Lighthouse worker requires Node.js >=22.19 (lighthouse@13.3.0 engines field)"
    env_vars: []
    dashboard_config:
      - task: "Install Node 22.19+ LTS (or 23.x) and run `cd lighthouse-worker && npm ci`"
        location: "Developer's machine; one-time. CLAUDE.md documents `node --version` ≥22.19 requirement"
must_haves:
  truths:
    - "A canned LH-13.3.0 fixture JSON can be normalized into a valid PageResult with all METRIC-01..05 fields populated without raising"
    - "Normalizing a LH JSON whose `lighthouseVersion` starts with `14.` raises ValueError citing the expected major (D-10)"
    - "Normalizing a fixture whose main-document waterfall item has statusCode=404 yields a PageResult with status_code=404 and perf_score=None / lcp_ms=None / cls=None / inp_proxy_tbt_ms=None (D-13)"
    - "page_slug(canonical_key('https://x.com/a/%2e%2e/b')) never contains '..', '/', or '\\\\' (D-07 / IN-02)"
    - "The Node worker exists at lighthouse-worker/run.mjs with lighthouse@13.3.0 pinned in package-lock.json and engines.node ≥22.19 declared"
    - "constants.py declares PER_SAMPLE_TIMEOUT_S, DEFAULT_SAMPLES_N, EXPECTED_LIGHTHOUSE_MAJOR_MINOR, INP_PROXY_DISPLAY_LABEL, DEVTOOLS_PORT_FILE_TIMEOUT_S, DEVTOOLS_PORT_POLL_INTERVAL_S, ALWAYS_INCLUDE_AUDITS, and ExitCode enum"
    - "Normalizer never constructs a local variable named `inp`, `inp_ms`, or `interaction_to_next_paint`; TBT writes directly into `inp_proxy_tbt_ms` (D-11 / D-15)"
  artifacts:
    - path: "src/perfcrawl/constants.py"
      provides: "Single-place tunables: timeouts, sample defaults, version gate string, INP label, exit codes (D-10, D-11, D-14, D-15)"
      contains: "PER_SAMPLE_TIMEOUT_S"
      contains_also:
        - "DEFAULT_SAMPLES_N"
        - "EXPECTED_LIGHTHOUSE_MAJOR_MINOR"
        - "INP_PROXY_DISPLAY_LABEL"
        - "DEVTOOLS_PORT_FILE_TIMEOUT_S"
        - "class ExitCode"
    - path: "src/perfcrawl/slug.py"
      provides: "IN-02-safe page_slug(url_key) function; defensive try/except + deterministic '_' fallback"
      exports: ["page_slug"]
    - path: "src/perfcrawl/normalizer.py"
      provides: "LH 13.3.0 JSON → PageResult transform with version gate, TBT→inp_proxy_tbt_ms mapping, curated diagnostics, partial-on-non-2xx"
      exports: ["normalize_lh"]
    - path: "lighthouse-worker/package.json"
      provides: "Node sibling project declaring lighthouse@13.3.0 exact + engines.node ≥22.19"
      contains: "\"lighthouse\": \"13.3.0\""
    - path: "lighthouse-worker/run.mjs"
      provides: "One-shot ESM Node worker: argv → lighthouse({port, ...}) → JSON-over-stdout"
    - path: "tests/fixtures/lighthouse/studyhalo-home-200.json"
      provides: "Real LH-13.3.0 capture of a 200 response (METRIC-01..05 source)"
    - path: "tests/fixtures/lighthouse/studyhalo-404.json"
      provides: "LH-13.3.0 capture of a non-2xx (D-13 partial-result fixture)"
    - path: "tests/fixtures/lighthouse/version-drift-14.json"
      provides: "Synthetic LH-14.0.0 fixture for D-10 version-gate test"
  key_links:
    - from: "src/perfcrawl/normalizer.py"
      to: "src/perfcrawl/constants.py"
      via: "import EXPECTED_LIGHTHOUSE_MAJOR_MINOR"
      pattern: "from perfcrawl.constants import EXPECTED_LIGHTHOUSE_MAJOR_MINOR"
    - from: "src/perfcrawl/normalizer.py"
      to: "src/perfcrawl/canonical.py"
      via: "import canonical_key"
      pattern: "from perfcrawl.canonical import canonical_key"
    - from: "src/perfcrawl/normalizer.py"
      to: "src/perfcrawl/models.py"
      via: "PageResult / MetricSample / WaterfallEntry construction"
      pattern: "PageResult\\(|MetricSample\\(|WaterfallEntry\\("
    - from: "tests/test_normalizer.py"
      to: "tests/fixtures/lighthouse/"
      via: "conftest fixture loaders (lh_home_200, lh_404, lh_version_14_drift)"
      pattern: "lh_home_200|lh_404|lh_version_14_drift"
---

<objective>
Establish the Phase 2 inside-out floor: pure-Python normalizer (LH 13.3.0 JSON → PageResult) + IN-02-safe page-slug + single-place constants module + the Node Lighthouse worker subproject + the canned LH JSON fixtures the rest of Phase 2 tests against.

Purpose: Front-load the two pieces of *real* engineering identified in RESEARCH § Summary (the LH JSON → PageResult normalizer and the IN-02 sanitization boundary) while they fit in one focused plan. Every subsequent plan (aggregator, orchestrator, CLI/output) consumes outputs of this one. Builds inside-out per RESEARCH § "Primary recommendation": normalizer first against canned LH JSON (no Node required), then layer on top.

Output: 8 new Python source files, 3 Lighthouse JSON fixtures, 2 new test modules, the `lighthouse-worker/` sibling project with `lighthouse@13.3.0` pinned and `npm ci`-able, and an updated `tests/conftest.py` exposing the LH fixtures to downstream tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-single-page-measurement-slice/02-CONTEXT.md
@.planning/phases/02-single-page-measurement-slice/02-RESEARCH.md
@.planning/phases/02-single-page-measurement-slice/02-PATTERNS.md
@.planning/phases/02-single-page-measurement-slice/02-VALIDATION.md
@.planning/phases/01-data-model-persistence-foundation/01-LEARNINGS.md
@src/perfcrawl/models.py
@src/perfcrawl/canonical.py
@src/perfcrawl/registry.py
@tests/conftest.py
@tests/test_canonical.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constants module + slug.py (IN-02 boundary) + their TDD tests</name>
  <files>src/perfcrawl/constants.py, src/perfcrawl/slug.py, tests/test_slug.py</files>
  <read_first>
    - src/perfcrawl/registry.py (THE "one editable place" precedent — same module docstring shape; per 02-PATTERNS § constants.py analog)
    - src/perfcrawl/canonical.py (defensive try/except + deterministic fallback; the IN-02 warning docstring on lines 72-82; the empty-input short-circuit on lines 84-89 — per 02-PATTERNS § slug.py analog)
    - tests/test_canonical.py (parametrize-over-malformed-inputs shape + deterministic-output assert; per 02-PATTERNS § test_slug.py analog)
    - .planning/phases/02-single-page-measurement-slice/02-PATTERNS.md § "src/perfcrawl/slug.py" (full slug body specification) and § "src/perfcrawl/constants.py" (full constants body)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § "Pattern 4: Slug sanitization (D-07, IN-02 boundary)" lines 500-544
    - .planning/phases/01-data-model-persistence-foundation/01-LEARNINGS.md § "Defensive try/except + deterministic fallback" (the pattern that 02-PATTERNS calls out as the slug.py template)
  </read_first>
  <behavior>
    - Test (test_slug.py): page_slug("") == "_"; page_slug("   ") == "_"; page_slug("\t\n") == "_" (parametrize over blanks; deterministic empty-sentinel mirrors test_canonical's blank handling).
    - Test (test_slug.py): page_slug("https://x.com/a/%2e%2e/b") → result satisfies (".." not in result) AND ("/" not in result) AND ("\\" not in result) AND (not result.startswith(".")). Same parametrize covers: "https://x.com/a/../b", "https://x.com/../../etc/passwd", "https://x.com/.../...//", "..", "../../../" (the IN-02 vectors from 02-PATTERNS test template).
    - Test (test_slug.py): page_slug returns charset subset of [A-Za-z0-9._-] for any input (regex assertion).
    - Test (test_slug.py): idempotent — page_slug(page_slug(x)) == page_slug(x) for representative URLs.
    - Test (test_slug.py): max_len truncation respected — page_slug("https://x.com/" + "a" * 200) has len ≤ 80 by default.
    - Test (test_slug.py): never raises on bizarre input — parametrize over `[None]` won't work since type-hint is str; instead parametrize over "", "://broken", "http://", " \t\n", "\x00\x01\x02" and assert result is a non-empty str matching the charset.
    - Test (test_constants.py NOT created; verified via import-and-assert in test_slug.py or in test_normalizer.py — see Task 3): from perfcrawl.constants import PER_SAMPLE_TIMEOUT_S, DEFAULT_SAMPLES_N, EXPECTED_LIGHTHOUSE_MAJOR_MINOR, INP_PROXY_DISPLAY_LABEL, ExitCode; PER_SAMPLE_TIMEOUT_S == 60; DEFAULT_SAMPLES_N == 3; EXPECTED_LIGHTHOUSE_MAJOR_MINOR == "13.x"; INP_PROXY_DISPLAY_LABEL == "INP (lab proxy, TBT-based)"; ExitCode.SUCCESS == 0; ExitCode.USER_ERROR == 1; ExitCode.MEASUREMENT_ERROR == 2.
  </behavior>
  <action>
    Create `src/perfcrawl/constants.py` per the full specification in 02-PATTERNS § "src/perfcrawl/constants.py". Module docstring opens with "Single-place tunables for Phase 2 measurement (D-02..D-16). Later phases extend these constants here only — call sites never inline a timeout, sample count, version string, exit code, or column label." Section banners follow registry.py style (`# --- D-XX: <name> (the ONE editable place) ---`). Declare module-level constants (per D-08/D-14/D-10/D-11 and Claude's-discretion): `PER_SAMPLE_TIMEOUT_S: int = 60` (D-14), `DEFAULT_SAMPLES_N: int = 3` (D-08 + Claude's discretion — odd-N is friendlier for median per 02-CONTEXT discretion list), `EXPECTED_LIGHTHOUSE_MAJOR_MINOR: str = "13.x"` (D-10), `INP_PROXY_DISPLAY_LABEL: str = "INP (lab proxy, TBT-based)"` (D-11), `DEVTOOLS_PORT_FILE_TIMEOUT_S: float = 5.0` and `DEVTOOLS_PORT_POLL_INTERVAL_S: float = 0.1` (RESEARCH Pitfall 1 — Claude's discretion), `ALWAYS_INCLUDE_AUDITS: frozenset[str] = frozenset({"interactive"})` (MEDIUM-4 fix from plan-check — Phase-2 carve-out from D-12 for the audits that source the CSV's `total_page_load_time` column; include unconditionally regardless of `score`). Declare `class ExitCode(IntEnum)` with members SUCCESS=0, USER_ERROR=1, MEASUREMENT_ERROR=2 (D-15) — the docstring notes "Phase 6 BUDG-01 will carve out 10+; the gap is intentional."

    Create `src/perfcrawl/slug.py` mirroring `canonical.py`'s module docstring shape (per 02-PATTERNS § slug.py). Module docstring opens with the function purpose, then a "Never raises on…" paragraph, then an enumerated list of failure modes. Cite **D-07 and the IN-02 landmine** in the opening lines so the threat model precedes the code. Implement `page_slug(url_key: str, *, max_len: int = 80) -> str` exactly as 02-PATTERNS specifies (`_SAFE = re.compile(r"[^A-Za-z0-9._-]+")`, `_DOTRUN = re.compile(r"\.{2,}")`, empty-input short-circuit returning `"_"`, try/except around the real work with `urlsplit` + replace `/` with `_` + collapse `..` runs via `_DOTRUN.sub("__", stem)` + `_SAFE.sub("_", stem)` + strip leading/trailing `._-` + truncate to `max_len`, deterministic `"_"` fallback in the except branch). Inline-comment idiom for the IN-02 guard: cite the LEARNINGS surprise verbatim as `"# IN-02: w3lib decodes %2e%2e to literal '../' in url_key; this is the documented sanitization boundary."`

    Create `tests/test_slug.py` per 02-PATTERNS § "tests/test_slug.py" — `test_no_path_traversal_in_slug` parametrized over the six IN-02 vectors listed in the behavior block, `test_empty_or_blank_input_returns_underscore_sentinel` parametrized over blanks, `test_charset_subset_of_safe_chars`, `test_idempotent`, `test_max_len_truncation`, `test_does_not_raise_on_bizarre_input`. Each test cites the source decision (D-07, IN-02) in a one-line docstring. Import style matches test_canonical.py.

    RED → GREEN discipline (per 02-PATTERNS § "TDD RED → GREEN commit pair per task" and Phase 1 LEARNINGS): write test_slug.py first, run `uv run pytest tests/test_slug.py -x`, confirm `ModuleNotFoundError: No module named 'perfcrawl.slug'` (or `perfcrawl.constants`) → first commit. Then create constants.py + slug.py, run the same test, confirm green → second commit.

    Critical invariant per D-07 / IN-02 / 02-PATTERNS: do NOT add any constant to `registry.py` (Phase 1 registry is closed); use `constants.py` for Phase 2 tunables. Do NOT redefine `TRACKING_PARAM_DENYLIST` or `Polarity` here (they're Phase 1's).
  </action>
  <verify>
    <automated>cd /Users/sneaky/JKL101/performance-statistics-gathering && uv run pytest tests/test_slug.py -x && uv run python -c "from perfcrawl.constants import PER_SAMPLE_TIMEOUT_S, DEFAULT_SAMPLES_N, EXPECTED_LIGHTHOUSE_MAJOR_MINOR, INP_PROXY_DISPLAY_LABEL, ExitCode; assert PER_SAMPLE_TIMEOUT_S == 60; assert DEFAULT_SAMPLES_N == 3; assert EXPECTED_LIGHTHOUSE_MAJOR_MINOR == '13.x'; assert INP_PROXY_DISPLAY_LABEL == 'INP (lab proxy, TBT-based)'; assert ExitCode.SUCCESS == 0 and ExitCode.USER_ERROR == 1 and ExitCode.MEASUREMENT_ERROR == 2; print('constants OK')" && uv run python -c "from perfcrawl.slug import page_slug; from perfcrawl.canonical import canonical_key; s = page_slug(canonical_key('https://x.com/a/%2e%2e/b')); assert '..' not in s and '/' not in s and '\\\\' not in s; print(repr(s))"</automated>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && ! grep -nE "^[[:space:]]+inp[[:space:]]*=" src/perfcrawl/slug.py src/perfcrawl/constants.py 2>/dev/null | grep -v "^[[:space:]]*#" | grep .</grep>
  </verify>
  <done>
    - `src/perfcrawl/constants.py` exists with all six named tunables + ExitCode IntEnum.
    - `src/perfcrawl/slug.py` exists exporting `page_slug(url_key, *, max_len=80) -> str`.
    - `tests/test_slug.py` exists; `uv run pytest tests/test_slug.py -x` green (≥5 tests passing).
    - The verify grep guard returns empty (no bare `inp = …` assignment in either new file).
    - Two commits visible in `git log`: `test(02-01): RED — failing tests for slug + constants` then `feat(02-01): IN-02-safe page_slug + Phase 2 constants module`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lighthouse worker subproject + LH fixtures + conftest plumbing</name>
  <files>lighthouse-worker/package.json, lighthouse-worker/package-lock.json, lighthouse-worker/run.mjs, lighthouse-worker/.gitignore, tests/conftest.py, tests/fixtures/lighthouse/studyhalo-home-200.json, tests/fixtures/lighthouse/studyhalo-404.json, tests/fixtures/lighthouse/version-drift-14.json, pyproject.toml</files>
  <read_first>
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § "Pattern 2: Node Lighthouse worker" lines 408-473 (the verbatim run.mjs body) and § "Installation" lines 144-167 (`npm init -y` + `npm install --save-exact lighthouse@13.3.0` + commit package-lock.json)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § "Pitfall 6: result.report shape changes with flags.output" (the destructure-when-array invariant)
    - .planning/phases/02-single-page-measurement-slice/02-PATTERNS.md § "Test fixtures: tests/fixtures/lighthouse/*.json" (capture-once convention) and § "tests/test_normalizer.py" → "For Phase 2 fixtures in conftest.py" (the new conftest fixture stubs)
    - .planning/phases/02-single-page-measurement-slice/02-CONTEXT.md § Implementation Decisions D-01, D-02, D-04 (worker boundary contract)
    - CLAUDE.md § "Version Compatibility" (lighthouse@13.3.0 requires Node ≥22.19; codify in engines field per D-04)
    - tests/conftest.py (existing fixture-loading pattern — `FIXTURES_DIR / "…"` + `read_text()`; add LH variants alongside existing run_v1 fixtures)
  </read_first>
  <behavior>
    - Test (verifiable by file inspection in `<verify>`): `lighthouse-worker/package.json` declares `"lighthouse": "13.3.0"` (exact, not `^`/`~`) under `dependencies` and `"engines": {"node": ">=22.19"}`.
    - Test: `lighthouse-worker/package-lock.json` exists and is committed (its presence is the lockfile contract per D-04).
    - Test: `lighthouse-worker/run.mjs` exists and contains the four key strings: `import lighthouse from "lighthouse"`, `parseArgs`, `flags.output` array containing `'json'` and `'html'`, and `JSON.stringify({` for the stdout write (i.e., the Pattern 2 shape).
    - Test: `tests/fixtures/lighthouse/studyhalo-home-200.json` exists, parses as JSON, has `lighthouseVersion` starting with `"13."`, has `categories.performance.score` (number 0-1), has `audits["network-requests"].details.items` (list, non-empty), has `audits["server-response-time"].numericValue`, has `audits["total-blocking-time"].numericValue`, has `audits["largest-contentful-paint"].numericValue`, has `audits["cumulative-layout-shift"].numericValue`, has `audits["total-byte-weight"].numericValue`, has `finalDisplayedUrl`.
    - Test: `tests/fixtures/lighthouse/studyhalo-404.json` exists, parses as JSON, has `lighthouseVersion` starting with `"13."`, and its main-document waterfall item (the one whose url matches `finalDisplayedUrl`) has `statusCode` 404.
    - Test: `tests/fixtures/lighthouse/version-drift-14.json` exists, parses as JSON, has `lighthouseVersion` starting with `"14."` (synthetic — copy of the home-200 fixture with only `lighthouseVersion` edited).
    - Test: `tests/conftest.py` exports three new fixtures `lh_home_200`, `lh_404`, `lh_version_14_drift`, each returning a `dict` parsed from the respective fixture file (per 02-PATTERNS conftest snippet).
    - Test: `tests/conftest.py` registers an `e2e` marker (`markers = ["e2e: end-to-end test requiring Node + Chrome + network"]`) — OR equivalent registration in `[tool.pytest.ini_options]`. Per RESEARCH Wave 0 Gaps.
  </behavior>
  <action>
    Create `lighthouse-worker/` sibling directory under repo root (NOT `src/lighthouse-worker/`). Inside, run `cd lighthouse-worker && npm init -y` to scaffold `package.json`, then edit it to: (a) set `"type": "module"` so `run.mjs` runs as ESM, (b) add `"dependencies": {"lighthouse": "13.3.0"}` with the EXACT version (no `^` or `~` per D-04), (c) add `"engines": {"node": ">=22.19"}` to hard-enforce the Node floor per CLAUDE.md Version Compatibility, (d) remove `main`/`scripts.test` defaults if `npm init` populated them with placeholders. Then `cd lighthouse-worker && npm install --save-exact lighthouse@13.3.0` to install + generate `package-lock.json`. Commit BOTH `package.json` and `package-lock.json`. (Per Open Q5: do NOT auto-invoke `npm ci` from Python; this is a documented one-time setup step. README is updated in Task 5 NOT here.)

    Add `lighthouse-worker/.gitignore` containing `node_modules/` (the install step creates the directory which must NOT be committed).

    Create `lighthouse-worker/run.mjs` per 02-RESEARCH § Pattern 2 verbatim. Use `node:util` `parseArgs` for argv (`port`, `url`, `form-factor` with default `"mobile"`). Import `lighthouse` from `"lighthouse"`. Build `flags = { port: Number(values.port), output: ["json", "html"], logLevel: "error" }`. Build `config` with `extends: "lighthouse:default"` and `settings.formFactor: values["form-factor"]`; when `form-factor === "desktop"`, ALSO override `settings.screenEmulation` (mobile: false, 1350x940, deviceScaleFactor 1, disabled: false) and `settings.throttling` (rttMs 40, throughputKbps 10240, cpuSlowdownMultiplier 1) per Pitfall 4 — the LH default config emulates mobile and `formFactor` alone is silently ignored. Run `await lighthouse(values.url, flags, config)`. Destructure `const [reportJson, reportHtml] = result.report` (the Pitfall 6 fix — `result.report` is a 2-array when `flags.output` is an array). Write `JSON.stringify({lhr: result.lhr, reportJson, reportHtml})` to `process.stdout`. Exit 0 on success; catch, write `worker error: ${err.message}` to `process.stderr`, exit 1 on failure. Also set a 55-second internal `setTimeout(() => process.exit(1), 55_000)` so the worker self-terminates even if Lighthouse stalls (per Assumption A5 — defense-in-depth for D-14).

    Generate the three Lighthouse fixtures:
    - `tests/fixtures/lighthouse/studyhalo-home-200.json` — a real LH-13.3.0 JSON capture against `https://example.com/` (mobile, default throttling). To generate: ensure node + Chromium are available (`uv run playwright install chromium` once), boot a Chrome via `node -e` or use the bundled chrome-launcher in the worker, run `node lighthouse-worker/run.mjs --port=<port> --url=https://example.com/ --form-factor=mobile`, capture the stdout JSON, extract ONLY the `lhr` key (`jq '.lhr'`), commit as `studyhalo-home-200.json`. The fixture MUST contain real LH 13.x audits — do NOT hand-write or stub it. Size: tens-to-hundreds of KB is expected and acceptable.
    - `tests/fixtures/lighthouse/studyhalo-404.json` — same generation flow against `https://example.com/__nope-404__` (a known-404 URL). The main-document `audits["network-requests"].details.items[]` entry must show `statusCode: 404`.
    - `tests/fixtures/lighthouse/version-drift-14.json` — synthetic: COPY `studyhalo-home-200.json` and edit only the top-level `lighthouseVersion` value to `"14.0.0"`. All other fields unchanged. This is the D-10 version-gate fixture.

    Update `tests/conftest.py` (do not rewrite — append fixtures). Add at the top of the existing file after the FIXTURES_DIR declaration:
    - `import json`
    - `LH_FIXTURES_DIR = FIXTURES_DIR / "lighthouse"`
    Then add three new pytest fixtures (per 02-PATTERNS snippet): `lh_home_200`, `lh_404`, `lh_version_14_drift`, each returning `json.loads((LH_FIXTURES_DIR / "<file>.json").read_text())`. Match the docstring style of the existing `run_v1_json` fixture.

    Register the e2e marker. Two options (pick ONE — Claude's discretion):
    - Option A: add `markers = ["e2e: end-to-end test requiring Node + Chrome + network"]` under `[tool.pytest.ini_options]` in `pyproject.toml`. (Recommended — Phase 1 already configures pytest there.)
    - Option B: use `pytest.ini_options` style in conftest's `pytest_configure` hook.

    Critical: do NOT add `lighthouse` (or any other Node package) to `pyproject.toml` — the PyPI `lighthouse` package is the abandoned 2016 service-discovery decoy per CLAUDE.md "What NOT to Use" and RESEARCH § Pitfall 8. The ONLY `lighthouse` reference in the entire repo lives in `lighthouse-worker/package.json` and `lighthouse-worker/package-lock.json`. The verify grep below enforces this invariant.
  </action>
  <verify>
    <automated>cd /Users/sneaky/JKL101/performance-statistics-gathering && test -f lighthouse-worker/package.json && test -f lighthouse-worker/package-lock.json && test -f lighthouse-worker/run.mjs && test -f lighthouse-worker/.gitignore && uv run python -c "import json; p = json.loads(open('lighthouse-worker/package.json').read()); assert p['dependencies']['lighthouse'] == '13.3.0', 'lighthouse pin'; assert p['type'] == 'module', 'esm'; assert p['engines']['node'].startswith('>=22'), 'node engines'; print('package.json OK')" && uv run python -c "import json; lh = json.loads(open('tests/fixtures/lighthouse/studyhalo-home-200.json').read()); assert lh['lighthouseVersion'].startswith('13.'), lh['lighthouseVersion']; assert 'performance' in lh['categories']; assert lh['audits']['network-requests']['details']['items']; assert lh['audits']['server-response-time']['numericValue'] is not None; assert lh['audits']['total-blocking-time']['numericValue'] is not None; print('home-200 OK', lh['lighthouseVersion'])" && uv run python -c "import json; lh = json.loads(open('tests/fixtures/lighthouse/studyhalo-404.json').read()); assert lh['lighthouseVersion'].startswith('13.'); items = lh['audits']['network-requests']['details']['items']; main = [i for i in items if i.get('url') == lh.get('finalDisplayedUrl')]; assert main and main[0]['statusCode'] == 404, f'expected 404 main doc, got {main}'; print('404 OK')" && uv run python -c "import json; lh = json.loads(open('tests/fixtures/lighthouse/version-drift-14.json').read()); assert lh['lighthouseVersion'].startswith('14.'), lh['lighthouseVersion']; print('drift OK', lh['lighthouseVersion'])" && uv run pytest --collect-only tests/conftest.py 2>&1 | grep -E "lh_home_200|lh_404|lh_version_14_drift" -c</automated>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && ! grep -nE "^[[:space:]]*[\"']lighthouse[\"']" pyproject.toml 2>/dev/null | grep -v '^[[:space:]]*#' | grep .</grep>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && grep -c '"lighthouse": "13.3.0"' lighthouse-worker/package.json</grep>
  </verify>
  <done>
    - `lighthouse-worker/{package.json,package-lock.json,run.mjs,.gitignore}` exist and committed.
    - `package.json` pins `lighthouse@13.3.0` exact AND declares `engines.node >=22.19` AND has `"type": "module"`.
    - Three LH fixtures exist under `tests/fixtures/lighthouse/`; home-200 + 404 have `lighthouseVersion: "13.x.y"` real captures; drift fixture has `"14.0.0"`.
    - `tests/conftest.py` exposes `lh_home_200`, `lh_404`, `lh_version_14_drift` (verified via `pytest --collect-only`).
    - e2e marker registered (in `pyproject.toml` or conftest).
    - The grep guard returns no `"lighthouse"` line in `pyproject.toml` (per Pitfall 8: never install the PyPI decoy).
    - One commit: `feat(02-01): Lighthouse worker subproject + LH-13.3.0 fixtures + conftest hooks`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Single-sample normalizer (LH JSON → PageResult) with version gate + D-13 partial</name>
  <files>src/perfcrawl/normalizer.py, tests/test_normalizer.py</files>
  <read_first>
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § "Normalizer skeleton" lines 700-782 (the verbatim normalize_lh body — the planner copies this shape verbatim) and § "Pattern 3: Version-gated normalizer" lines 476-498 (the version-gate function body)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § "Pitfall 2: LH 13 renamed waterfall timing keys" (rendererStartTime / networkRequestTime / networkEndTime — load-bearing for METRIC-03)
    - .planning/phases/02-single-page-measurement-slice/02-PATTERNS.md § "src/perfcrawl/normalizer.py" (the version gate cite, per-field `_numeric`/`_cat_score` helpers, the labeled-INP-proxy invariant — "Never name a local variable inp or inp_ms")
    - .planning/phases/02-single-page-measurement-slice/02-PATTERNS.md § "tests/test_normalizer.py" (test naming pattern test_<thing>_<expected_behavior>, parametrize style, fixture loading)
    - src/perfcrawl/models.py (PageResult / MetricSample / WaterfallEntry shapes; the `_no_bare_inp` model-layer floor on lines 152-166 — defense-in-depth proves the labeling enforcement)
    - src/perfcrawl/canonical.py (the IN-02 warning docstring — normalizer sets `url_key` via `canonical_key(url_as_measured)`, NEVER constructs a filesystem path from it)
    - .planning/phases/01-data-model-persistence-foundation/01-LEARNINGS.md § "Labeled-INP-proxy invariant enforced at the model layer" (every layer refuses to surface a bare `inp` name)
    - tests/test_canonical.py (parametrize + fixture loading pattern — the test naming style mirror)
  </read_first>
  <behavior>
    For each Phase Requirement, define one named test that consumes a fixture from Task 2 and asserts the mapped behavior:

    - `test_category_scores_mapped` (METRIC-01): `normalize_lh(lh_home_200, url_as_measured="https://example.com/")` yields a PageResult whose `perf_score`, `a11y_score`, `seo_score`, `best_practices_score` are non-None floats between 0.0 and 100.0 (the normalizer multiplies LH's 0-1 score by 100). Cross-check: the per-field value equals `lh_home_200["categories"][cat]["score"] * 100`.
    - `test_cwv_mapping` (METRIC-02 + D-11/D-15): the returned PageResult has `lcp_ms.median` equal to `lh_home_200["audits"]["largest-contentful-paint"]["numericValue"]`, `cls.median` equal to `lh_home_200["audits"]["cumulative-layout-shift"]["numericValue"]`, AND `inp_proxy_tbt_ms.median` equal to `lh_home_200["audits"]["total-blocking-time"]["numericValue"]`. Negative assertion: the normalizer module's source text (read via `inspect.getsource`) contains zero matches for `r"\binp\s*="` or `r"\binp_ms\s*="` or `r"interaction_to_next_paint\s*="` (the labeled-proxy invariant from D-15 + Phase 1 LEARNINGS — defense-in-depth above the model-layer floor).
    - `test_waterfall_timing_uses_lh13_keys` (METRIC-03 + Pitfall 2): every `WaterfallEntry` in the returned `PageResult.waterfall` whose source item has both `networkRequestTime` AND `networkEndTime` carries a non-None `timing_ms` equal to `end - start`. Items missing either key carry `timing_ms=None` (not raised, not crashed — defensive). The waterfall is non-empty (METRIC-03).
    - `test_network_facts` (METRIC-04): `ttfb_ms.median` equals `audits["server-response-time"].numericValue`; `total_bytes` equals `int(audits["total-byte-weight"].numericValue)`; `request_count` equals `len(waterfall)`; `status_code` equals the `statusCode` of the waterfall item whose `url == lhr["finalDisplayedUrl"]`; `slowest_request_url` and `slowest_request_ms` match `max(waterfall, key=timing_ms)` (over items with a non-None `timing_ms`).
    - `test_diagnostics_curated` (METRIC-05 + D-12 with MEDIUM-4 carve-out): for every audit ID in `result.diagnostics`, either `entry["score"] < 1` OR the ID is in `ALWAYS_INCLUDE_AUDITS` (currently `{"interactive"}`); every audit ID in `lh_home_200["audits"]` whose `score >= 1` AND not in `ALWAYS_INCLUDE_AUDITS` is ABSENT from `result.diagnostics`. The dict is non-empty for the home-200 fixture (real audits will have some failures).
    - `test_diagnostics_always_includes_interactive` (MEDIUM-4 carve-out — supports OUT-04 `total_page_load_time`): even with a synthetic LH JSON where `audits["interactive"]["score"] == 1` (a healthy page), `result.diagnostics["interactive"]["numericValue"]` is present and numeric. Use `monkeypatch` or a constructed dict to set `score=1` while preserving `numericValue`.
    - `test_version_gate_rejects_major_drift` (D-10): `normalize_lh(lh_version_14_drift, url_as_measured="https://example.com/")` raises `ValueError`; the exception message contains both `"14"` and the expected major (`"13"`) from `EXPECTED_LIGHTHOUSE_MAJOR_MINOR`. Imported from `perfcrawl.constants`.
    - `test_partial_result_on_non_2xx` (D-13): `normalize_lh(lh_404, url_as_measured="https://example.com/__nope-404__")` returns a PageResult where `status_code == 404`. METRIC-04 facts (request_count, total_bytes, status_code, ttfb_ms) MAY be present if Lighthouse captured them. The contract per D-13 is `status_code` is recorded; LH category scores and CWV fields may be null. Assertion: `result.status_code == 404` (the load-bearing assertion). DO NOT assert `perf_score is None` rigidly — D-13 says "stay null" but real LH 13 may produce partial category scores even for 404s; assert `result.status_code == 404` and document that field-nullness of categories is LH-driven, not normalizer-driven.
    - `test_url_key_set_via_canonical_key`: the returned PageResult's `url_key` equals `canonical_key(url_as_measured)`. Used by Phase 6 cross-run self-join.
    - `test_throttling_stamp` (RUN-02 — partial coverage in this plan; full stamp lands in Task 4): SKIP for now; throttling stamp is filled by the orchestrator from worker output in plan 02-03. Note in test_normalizer.py file docstring "RUN-02 throttling stamp is set by orchestrator in 02-03, not by normalizer."
  </behavior>
  <action>
    Create `src/perfcrawl/normalizer.py` per the verbatim skeleton in 02-RESEARCH § "Normalizer skeleton" (lines 700-782) and 02-PATTERNS § "src/perfcrawl/normalizer.py". Public surface: ONE function `normalize_lh(lhr: dict, *, url_as_measured: str) -> PageResult` returning a fully-populated single-sample PageResult.

    Module docstring opens with: "Lighthouse-JSON → PageResult normalizer (Phase 2 D-09/D-10/D-11/D-12/D-13). Single source of truth for LH 13.3.0 audit-shape interpretation; downstream code reads only `PageResult`, never raw `lhr`." Then a short paragraph explaining the version-gate rationale (per D-10 — silent audit-shape drift on a 14.x lockfile bump is the realistic failure mode this gate prevents). Then a top-of-file `# INVARIANT: never construct a local variable named 'inp', 'inp_ms', or 'interaction_to_next_paint'. TBT writes DIRECTLY into inp_proxy_tbt_ms (D-11/D-15). The model-layer _no_bare_inp validator is the floor; this normalizer should never approach it.` comment.

    Private helper `_check_version(lhr: dict) -> None`: hard-error per 02-RESEARCH Pattern 3. Read `lhr.get("lighthouseVersion", "")`; extract major via `expected_major = EXPECTED_LIGHTHOUSE_MAJOR_MINOR.split(".")[0]`; raise `ValueError` with the actual-vs-expected version when `not actual.startswith(expected_major + ".")`. Message format: `f"Lighthouse version mismatch: expected major {expected_major}.x, got {actual!r}. Normalizer is locked to LH {EXPECTED_LIGHTHOUSE_MAJOR_MINOR} audit shape; refusing to silently produce a corrupted PageResult."` (mirrors Pattern 3 verbatim). Import `EXPECTED_LIGHTHOUSE_MAJOR_MINOR` from `perfcrawl.constants`.

    Private helpers `_numeric(audits, audit_id) -> float | None` and `_cat_score(cats, key) -> float | None` per 02-PATTERNS — chained `.get()` so missing keys never raise; convert to `float(score * 100)` for category scores (LH 0-1 → 0-100 scale to match the existing Sheets workflow per 02-CONTEXT § Specifics).

    Build the waterfall by iterating `audits.get("network-requests", {}).get("details", {}).get("items", [])`. For each item:
    - `start = item.get("networkRequestTime")` (NOT `startTime` — Pitfall 2!)
    - `end = item.get("networkEndTime")` (NOT `endTime`!)
    - `timing = (end - start) if (start is not None and end is not None) else None`
    - Construct `WaterfallEntry(url, resource_type=item.get("resourceType"), size_bytes=item.get("transferSize"), timing_ms=timing, status_code=item.get("statusCode"))`. (`WaterfallEntry`'s `extra="ignore"` quietly drops the other LH keys per Phase 1 model contract.)
    - Track the main document via `item.get("url") == lhr.get("finalDisplayedUrl")` for the page-level `status_code` (D-13).

    Compute the slowest request via `max((w for w in waterfall if w.timing_ms is not None), key=lambda w: w.timing_ms, default=None)`.

    Build `diagnostics` per D-12 with one explicit carve-out (MEDIUM-4 fix from plan-check): `{aid: a for aid, a in audits.items() if (a.get("score") is not None and a["score"] < 1) or aid in ALWAYS_INCLUDE_AUDITS}` where `ALWAYS_INCLUDE_AUDITS = frozenset({"interactive"})` is defined in `constants.py`. The `interactive` audit is included REGARDLESS of `score` because it sources the CSV `total_page_load_time` column (OUT-04 in 02-04), which would otherwise be empty on fast pages that score `1.0` on `interactive`. If the resulting dict is empty, set `diagnostics=None` rather than an empty dict (Phase 1 model accepts `dict | None`). Document the carve-out with an inline comment citing OUT-04.

    Construct the final `PageResult` with: `url=url_as_measured`, `url_key=canonical_key(url_as_measured)`, all four category scores, `MetricSample(median=_numeric(audits, "..."), samples=[_numeric(audits, "...")] if _numeric(audits, "...") is not None else [])` for `lcp_ms`/`cls`/`inp_proxy_tbt_ms`/`ttfb_ms` (single-sample shape — the aggregator in 02-02 collects across N samples), `request_count=len(waterfall)`, `total_bytes=int(...)` (guard for None), `status_code=main_doc_status`, slowest request fields, the waterfall list, and `diagnostics=diagnostics`. `analysis=None` (Phase 5 fills).

    Critical labeled-INP-proxy invariant (per D-11/D-15 + 02-PATTERNS): the TBT read goes DIRECTLY into the `inp_proxy_tbt_ms=` keyword argument; do NOT bind it to a local named `inp` or `inp_ms` first. The grep guard in `<verify>` enforces this.

    Create `tests/test_normalizer.py` per 02-PATTERNS § "tests/test_normalizer.py". Module docstring lists each requirement and its named test. Tests load fixtures via the `lh_home_200`, `lh_404`, `lh_version_14_drift` conftest fixtures added in Task 2. Test names match the behavior block 1:1.

    RED → GREEN: write `tests/test_normalizer.py` first, run `uv run pytest tests/test_normalizer.py -x`, confirm `ImportError: cannot import name 'normalize_lh'` → commit `test(02-01): RED — failing normalizer tests against LH-13 fixtures`. Then create `src/perfcrawl/normalizer.py`, run the same test, confirm green → commit `feat(02-01): LH-13.3.0 → PageResult normalizer with D-10 gate + D-13 partial`.

    Defense-in-depth grep test (per 02-PATTERNS "Labeled-proxy invariant" + Phase 1 LEARNINGS grep-assert precedent): include a meta-test `test_normalizer_source_has_no_bare_inp` inside `test_normalizer.py` that reads `inspect.getsource(perfcrawl.normalizer)` and asserts no `re.search(r"\binp\b", source)` match other than the substring `inp_proxy_tbt_ms`. (Implementation hint: `re.findall(r"\binp\b(?!_proxy)", source) == []`. Plus assert `inp_proxy_tbt_ms` appears at least once.)
  </action>
  <verify>
    <automated>cd /Users/sneaky/JKL101/performance-statistics-gathering && uv run pytest tests/test_normalizer.py tests/test_slug.py -x && uv run python -c "import json; from perfcrawl.normalizer import normalize_lh; lh = json.loads(open('tests/fixtures/lighthouse/studyhalo-home-200.json').read()); p = normalize_lh(lh, url_as_measured='https://example.com/'); assert p.url == 'https://example.com/'; assert p.url_key; assert p.perf_score is not None; assert p.lcp_ms is not None; assert p.inp_proxy_tbt_ms is not None; assert p.waterfall; print('normalize OK', p.perf_score, p.lcp_ms.median, p.inp_proxy_tbt_ms.median)" && uv run python -c "import json; from perfcrawl.normalizer import normalize_lh; lh = json.loads(open('tests/fixtures/lighthouse/version-drift-14.json').read()); raised=False; \nexec(\"try:\\n    normalize_lh(lh, url_as_measured='https://x/')\\nexcept ValueError as e:\\n    raised=True; assert '14' in str(e) and '13' in str(e); print('gate OK', str(e)[:80])\"); assert raised, 'version gate did not raise'"</automated>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && python3 -c "import re; src = open('src/perfcrawl/normalizer.py').read(); bare = [m for m in re.findall(r'\binp\b(?!_proxy)', src)]; assert bare == [], f'bare-INP found in normalizer.py: {bare}'; assert 'inp_proxy_tbt_ms' in src, 'inp_proxy_tbt_ms missing'; print('labeling invariant OK')"</grep>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && grep -c "networkRequestTime\|networkEndTime" src/perfcrawl/normalizer.py</grep>
  </verify>
  <done>
    - `src/perfcrawl/normalizer.py` exists exporting `normalize_lh(lhr, *, url_as_measured) -> PageResult`.
    - `tests/test_normalizer.py` exists with ≥8 tests; `uv run pytest tests/test_normalizer.py -x` green.
    - The labeling-invariant grep guard passes: no bare `inp` token outside `inp_proxy_tbt_ms` substring.
    - The waterfall-keys grep finds `networkRequestTime` and `networkEndTime` (NOT the pre-LH-12 `startTime`/`endTime`) — Pitfall 2 protection.
    - `normalize_lh(lh_version_14_drift, ...)` raises ValueError citing 14 and 13 (D-10 gate).
    - `normalize_lh(lh_404, ...)` returns PageResult with `status_code == 404` (D-13).
    - Two commits visible: `test(02-01): RED — failing normalizer tests` then `feat(02-01): LH-13.3.0 → PageResult normalizer with D-10 gate + D-13 partial`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-controlled URL → page_slug | Untrusted URL (potentially containing decoded `%2e%2e` per IN-02) crosses into filesystem-path construction at slug.py |
| Untrusted LH JSON → PageResult | Worker stdout (a string from the Node subprocess) is parsed and converted into a typed model; an attacker who controlled `lighthouse-worker/run.mjs` output could supply hostile audit data |
| Repository dependency boundary | `pyproject.toml` and `lighthouse-worker/package.json` are the two surfaces an attacker could exploit via slopsquatting (the abandoned PyPI `lighthouse` package is the documented threat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `slug.py::page_slug` (URL → filesystem stem) | mitigate | IN-02 landmine — `_DOTRUN` collapses `..` runs, `_SAFE` charset-restricts to `[A-Za-z0-9._-]`, leading-dot strip prevents hidden-file names. Test `test_no_path_traversal_in_slug` parametrizes over 6 traversal vectors and grep-asserts `'..' not in slug` (per D-07 / 02-RESEARCH Security Domain) |
| T-02-02 | Tampering | `normalizer.py` LH-JSON parser | mitigate | D-10 version gate raises `ValueError` on any LH major drift (`14.x`, `12.x`, missing version) before parsing audits — prevents silent audit-shape corruption from a compromised or version-drifted worker. Pydantic `extra="ignore"` + `allow_inf_nan=False` on PageResult/MetricSample are the model-layer floor (`test_version_gate_rejects_major_drift`) |
| T-02-SC | Tampering | `pyproject.toml` (slopsquat risk) | mitigate | Per CLAUDE.md "What NOT to Use" + Pitfall 8: the PyPI `lighthouse` package is the abandoned 2016 service-discovery decoy. The grep guard in Task 2 verify section asserts no `"lighthouse"` entry exists in `pyproject.toml`; the ONLY `lighthouse` reference lives in `lighthouse-worker/package.json` (npm namespace — the legitimate Google Lighthouse). Phase 2 introduces no new PyPI packages (only Phase 1 deps remain) — `playwright`/`typer`/`rich` ship in plan 02-03 / 02-04 |
| T-02-N | Spoofing | INP labeling drift | mitigate | Labeled-proxy invariant defense-in-depth: (a) model-layer `_no_bare_inp` validator (Phase 1 — the floor), (b) normalizer.py grep-asserted "no `inp` token outside `inp_proxy_tbt_ms`" (Task 3 `<verify>`), (c) `INP_PROXY_DISPLAY_LABEL` constant declared in constants.py. Three independent layers; a regression must break all three to surface a bare INP value |
| T-02-D | Disclosure | `tests/fixtures/lighthouse/` (committed real-LH captures from `example.com`) | accept | The fixtures contain only Lighthouse audit output from a public well-known URL (`example.com`) — no credentials, PII, internal hostnames, or session cookies. `example.com` is IANA-reserved for testing purposes; nothing under it is sensitive. Should the team ever capture a fixture from `studyhalo.com` (their own dashboards), that capture goes through a separate human-review checkpoint (per Phase 4 auth-handling boundary, not this plan) |

ASVS coverage: V5 (Input Validation — URL→slug, LH-version gate), V12 (Files and Resources — IN-02 path-traversal protection), V14 (Configuration — `lighthouse-worker/package-lock.json` committed for byte-identical npm installs).
</threat_model>

<verification>
After all three tasks:

```bash
cd /Users/sneaky/JKL101/performance-statistics-gathering
uv run pytest tests/test_slug.py tests/test_normalizer.py -x
# All Phase 1 tests still green:
uv run pytest -x
# Normalizer round-trips a real LH-13 fixture into a valid PageResult:
uv run python -c "
import json
from perfcrawl.normalizer import normalize_lh
lh = json.loads(open('tests/fixtures/lighthouse/studyhalo-home-200.json').read())
p = normalize_lh(lh, url_as_measured='https://example.com/')
assert p.perf_score is not None
assert p.lcp_ms is not None and p.lcp_ms.median is not None
assert p.inp_proxy_tbt_ms is not None  # D-11 labeled
assert p.waterfall
print('floor OK:', p.url_key, p.perf_score, 'TBT(lab INP proxy):', p.inp_proxy_tbt_ms.median)
"
# Lighthouse worker is installable:
cd lighthouse-worker && npm ci && cd ..
ls lighthouse-worker/node_modules/lighthouse/package.json
```
</verification>

<success_criteria>
- All listed Phase Requirements (METRIC-01..05, RUN-01, RUN-02) have at least one passing test in `tests/test_normalizer.py` referencing a real LH-13 fixture.
- D-07 (IN-02), D-10 (version gate), D-11 (TBT→inp_proxy_tbt_ms), D-12 (curated diagnostics), D-13 (partial-on-non-2xx), D-14/D-15 (constants exist) are each cited by name in code or test docstrings (traceability).
- `uv run pytest tests/test_slug.py tests/test_normalizer.py -x` green (≥13 named tests).
- `uv run pytest` (full Phase 1 + Phase 2 partial) green — no regression on Phase 1 contract.
- `cd lighthouse-worker && npm ci` succeeds on a Node ≥22.19 machine (the worker boundary is implementable).
- The two defense-in-depth grep guards (`pyproject.toml` has no `lighthouse` line; `normalizer.py` has no bare `inp` token) return empty / pass.
</success_criteria>

<output>
Create `.planning/phases/02-single-page-measurement-slice/02-01-SUMMARY.md` when done.
</output>
