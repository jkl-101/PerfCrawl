---
phase: 02-single-page-measurement-slice
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
files_modified:
  - pyproject.toml
  - .gitignore
  - src/perfcrawl/output.py
  - src/perfcrawl/cli.py
  - tests/test_output.py
  - tests/test_cli.py
  - tests/test_e2e.py
autonomous: true
requirements:
  - OUT-03
  - OUT-04
  - CLI-01
user_setup: []
must_haves:
  truths:
    - "perfcrawl measure URL --samples 1 (no --json) prints a Rich human-readable table to stdout listing perf/a11y/seo/best-practices scores, LCP, CLS, `INP (lab proxy, TBT-based)`, TTFB, request count, total bytes, slowest request URL+ms, status code; progress output goes to stderr"
    - "perfcrawl measure URL --samples 1 --json prints a single full-fidelity RunRecord JSON to stdout (parseable by `json.loads`); no Rich table on stdout; progress still on stderr"
    - "perfcrawl measure URL exits 0 on successful measurement (including D-13 partial-on-non-2xx), 1 on user error (empty URL, malformed flags, can't write output dir), 2 on measurement error (all samples failed, Chrome won't launch, lighthouse-worker not installed)"
    - "After a successful run, the on-disk layout is output/<run_id>/result.json + output/<run_id>/result.csv + output/<run_id>/lighthouse/<page-slug>.json + output/<run_id>/lighthouse/<page-slug>.html; <run_id> matches RunRecord.id; <page-slug> goes through page_slug() (IN-02 boundary)"
    - "result.csv first row contains the locked CSV_COLUMNS in order; result.csv second row contains the measured PageResult flattened to the same column order"
    - "result.json reads back via RunRecord.model_validate_json(...) into a model equal to the original RunRecord (round-trip identity, mirroring Phase 1's hybrid store contract)"
    - "The RunRecord is also written to the Phase 1 SQLite store via store.write_run, so the on-disk artifacts and the DB row share one run_id (D-07)"
    - "output/ is in .gitignore so committed repo never carries runtime artifacts (Claude's discretion per 02-CONTEXT — recommended YES)"
  artifacts:
    - path: "src/perfcrawl/output.py"
      provides: "On-disk artifact writers: write_outputs(run_record, raw_lh_json, raw_lh_html, output_dir) writes result.json + result.csv + lighthouse/<slug>.{json,html}; CSV_COLUMNS locked column list."
      exports: ["write_outputs", "CSV_COLUMNS"]
    - path: "src/perfcrawl/cli.py"
      provides: "Typer app with `measure` subcommand; argv → orchestrator.measure_url → output writers → stdout (Rich or JSON) with D-15 exit codes."
      exports: ["app", "measure"]
    - path: "pyproject.toml"
      provides: "typer>=0.15 and rich>=13 added as runtime dependencies; [project.scripts] perfcrawl = 'perfcrawl.cli:app' entry point restored (Phase 1 deliberately removed it; Phase 2 brings it back)."
      contains: "typer>=0.15"
      contains_also:
        - "rich>=13"
        - "[project.scripts]"
        - "perfcrawl.cli:app"
    - path: ".gitignore"
      provides: "output/ entry so runtime artifacts are never committed"
      contains: "output/"
  key_links:
    - from: "src/perfcrawl/cli.py"
      to: "src/perfcrawl/orchestrator.py"
      via: "measure_url() invocation; UserError → ExitCode.USER_ERROR; MeasurementError → ExitCode.MEASUREMENT_ERROR"
      pattern: "from perfcrawl.orchestrator import measure_url"
    - from: "src/perfcrawl/cli.py"
      to: "src/perfcrawl/output.py"
      via: "write_outputs(run_record, ...) call after successful measurement"
      pattern: "from perfcrawl.output import write_outputs"
    - from: "src/perfcrawl/cli.py"
      to: "src/perfcrawl/store.py"
      via: "write_run(conn, run_record) persistence call (Phase 1 store, unchanged)"
      pattern: "from perfcrawl.store import write_run|init_db"
    - from: "src/perfcrawl/cli.py"
      to: "src/perfcrawl/constants.py"
      via: "import ExitCode, INP_PROXY_DISPLAY_LABEL, DEFAULT_SAMPLES_N"
      pattern: "from perfcrawl.constants import"
    - from: "src/perfcrawl/output.py"
      to: "src/perfcrawl/slug.py"
      via: "page_slug(page.url_key) for the per-page LH artifact filename — IN-02 boundary"
      pattern: "from perfcrawl.slug import page_slug"
---

<objective>
Wire the proven plumbing of 02-01..03 into a single command: `perfcrawl measure <url> [--samples N] [--emulation mobile|desktop] [--json] [--output-dir output]`. The CLI invokes the orchestrator, writes outputs to disk via `output.py`, persists the run to the Phase 1 SQLite store, and renders either a Rich human-readable table (default) or the full RunRecord JSON (`--json`) on stdout with progress / errors always on stderr.

Purpose: Satisfies CLI-01 (machine-readable + non-interactive), OUT-03 (raw LH JSON+HTML per page), OUT-04 (flat CSV one-row-per-page + full-fidelity JSON), D-05 (subcommand verb), D-06 (human-readable default + `--json` opt-in + stderr for progress), D-07 (on-disk layout), D-15 (exit codes 0/1/2). This is the seal-the-vertical-slice plan: after it, a developer types `perfcrawl measure https://example.com` and gets a real measurement on disk and in SQLite.

Output: Two new Python modules (`output.py`, `cli.py`) + three new test modules (`test_output.py`, `test_cli.py`, `test_e2e.py`) + `pyproject.toml` dependency adds (typer, rich) + `[project.scripts]` entry-point restoration + `.gitignore` augmentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-single-page-measurement-slice/02-CONTEXT.md
@.planning/phases/02-single-page-measurement-slice/02-RESEARCH.md
@.planning/phases/02-single-page-measurement-slice/02-PATTERNS.md
@.planning/phases/02-single-page-measurement-slice/02-01-SUMMARY.md
@.planning/phases/02-single-page-measurement-slice/02-03-SUMMARY.md
@.planning/phases/01-data-model-persistence-foundation/01-LEARNINGS.md
@src/perfcrawl/orchestrator.py
@src/perfcrawl/store.py
@src/perfcrawl/normalizer.py
@src/perfcrawl/slug.py
@src/perfcrawl/constants.py
@src/perfcrawl/models.py
@.gitignore
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: output.py — CSV + JSON + raw-LH-artifact writers with IN-02-safe slug + locked CSV_COLUMNS</name>
  <files>src/perfcrawl/output.py, tests/test_output.py</files>
  <read_first>
    - .planning/phases/02-single-page-measurement-slice/02-PATTERNS.md § "src/perfcrawl/output.py" lines 460-503 (the analog to store.py atomic-write pattern; the CSV_COLUMNS locked list; the IN-02 boundary call to page_slug)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § "CSV column order (OUT-04)" lines 848-881 (the verbatim CSV_COLUMNS list — DO NOT reorder or rename)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § Open Q1 (single-artifact-per-page default — `output/<run_id>/lighthouse/<slug>.{json,html}`, NOT N artifacts; the persisted JSON carries all N samples in MetricSample.samples) and Open Q2 (page column empty for Phase 2) and Open Q4 (total_page_load_time = `audits['interactive'].numericValue` TTI proxy)
    - .planning/phases/02-single-page-measurement-slice/02-CONTEXT.md § D-07 (output layout + IN-02 path-traversal protection via page_slug)
    - .planning/phases/02-single-page-measurement-slice/02-PATTERNS.md § "tests/test_output.py" lines 845-902 (test_round_trip_identity analog + test_csv_column_order + test_raw_artifacts_on_disk)
    - src/perfcrawl/store.py lines 134-154 (atomic-write pattern — the file-I/O analog for output.py is `tempfile.NamedTemporaryFile(dir=...) + os.replace`)
    - src/perfcrawl/slug.py (the IN-02 boundary call — every `output/<run_id>/lighthouse/<slug>` path goes through `page_slug(page.url_key)`, never the raw url_key)
    - src/perfcrawl/models.py (PageResult.url_key warning docstring on lines 73-82 — the IN-02 sanitization-boundary contract this module honors)
    - tests/conftest.py § sample_run fixture lines 51-92 (the Phase 1 RunRecord fixture used as test data here)
  </read_first>
  <behavior>
    - `test_json_round_trip` (OUT-04 + Phase 1 hybrid-store analog): `write_outputs(sample_run, output_dir=tmp_path, raw_lh_json=None, raw_lh_html=None)` writes `tmp_path/<run_id>/result.json`. Reading that file via `RunRecord.model_validate_json(Path(...).read_text())` returns a RunRecord whose `model_dump()` equals `sample_run.model_dump()` (model equality — the same contract Phase 1's `test_round_trip_identity` proves for the SQLite store).
    - `test_csv_column_order` (OUT-04): the first line of `tmp_path/<run_id>/result.csv` split on `,` equals the `CSV_COLUMNS` list exactly (in order, no extra columns, no missing columns). The second line has one cell per column.
    - `test_csv_inp_proxy_column_is_labeled` (D-11 + D-15 + Phase 1 LEARNINGS labeled-proxy invariant): the CSV header contains `inp_proxy_tbt_ms` (the field name itself is the labeling — the column header name is the signal, not a separate label string). The header does NOT contain a bare `"inp"` token (parametrize over candidate forbidden headers: `"inp"`, `"inp_ms"`, `"interaction_to_next_paint"`).
    - `test_csv_values_round_trip` (OUT-04): each cell in the CSV second row maps to the correct `PageResult`/`RunRecord` field for the sample_run fixture. Spot-check: `run_id` cell == `str(sample_run.id)`; `lcp_ms` cell == `str(sample_run.pages[0].lcp_ms.median)` (or empty string if `None`); `cache_disabled` cell == `"TRUE"` (per RESEARCH CSV column comments — RUN-03 is always cold cache).
    - `test_raw_lh_artifacts_on_disk` (OUT-03): `write_outputs(sample_run, output_dir=tmp_path, raw_lh_json='{"lhr":{}}', raw_lh_html='<html/>')` creates `tmp_path/<run_id>/lighthouse/<slug>.json` AND `tmp_path/<run_id>/lighthouse/<slug>.html` where `<slug> == page_slug(sample_run.pages[0].url_key)`. Both files contain the exact bytes passed in.
    - `test_raw_lh_artifacts_skipped_when_none`: `write_outputs(sample_run, output_dir=tmp_path, raw_lh_json=None, raw_lh_html=None)` does NOT create the `lighthouse/` subdirectory (or creates it empty — choose one; document the choice).
    - `test_slug_in_artifact_path_never_traverses` (D-07 / IN-02 reaffirmation): build a PageResult whose `url_key` is `"https://x.com/a/../b"` (literal `..` from the IN-02 landmine); `write_outputs(run_with_traversal_page, ...)` produces an artifact path whose stem matches the `[A-Za-z0-9._-]` charset and has no `..` substring. The written file lives strictly under `tmp_path/<run_id>/lighthouse/`.
    - `test_output_dir_created_when_missing`: `write_outputs(sample_run, output_dir=tmp_path / "deep" / "nested")` creates the missing intermediate directories (mkdir parents=True, exist_ok=True).
    - `test_output_dir_unwriteable_raises_user_error`: when `output_dir` cannot be created (e.g. parent is a file, not a dir), `write_outputs` raises `OSError` (which the CLI in Task 2 maps to `ExitCode.USER_ERROR` per D-15). Use a tmp_path-based stand-in (create a regular file at the path the dir would go).
  </behavior>
  <action>
    Create `src/perfcrawl/output.py` per 02-PATTERNS § "src/perfcrawl/output.py":

    Module docstring opens with: "On-disk artifact writers for Phase 2 (OUT-03/OUT-04, D-07). Writes:\n- `<output_dir>/<run_id>/result.json` — full-fidelity RunRecord (RunRecord.model_dump_json) — round-trip-identical to the SQLite blob.\n- `<output_dir>/<run_id>/result.csv` — flat one-row-per-page CSV in the locked CSV_COLUMNS order (a superset of the existing studyhalo Google Sheet schema; Phase 6 Sheets exporter reads the same shape).\n- `<output_dir>/<run_id>/lighthouse/<page-slug>.{json,html}` — raw Lighthouse JSON + HTML artifacts per page, with `<page-slug>` going through page_slug() (IN-02 boundary)."

    Imports: `import csv, os, tempfile`, `from pathlib import Path`, `from datetime import datetime`. Plus `from perfcrawl.models import RunRecord, PageResult, MetricSample`, `from perfcrawl.slug import page_slug`.

    Declare `CSV_COLUMNS: list[str]` per 02-RESEARCH § "CSV column order (OUT-04)" verbatim:
    ```
    CSV_COLUMNS: list[str] = [
        "page", "url", "test_date", "cache_disabled",
        "total_page_load_time", "request_count", "total_bytes",
        "slowest_request_url", "slowest_request_ms", "ttfb_ms", "status_code",
        "perf_score", "a11y_score", "seo_score", "best_practices_score",
        "lcp_ms", "cls", "inp_proxy_tbt_ms",
        "schema_version", "run_id", "chrome_version", "lighthouse_version", "emulation",
    ]
    ```
    Place a module-level banner before it: `# --- OUT-04: the CSV column order (the ONE editable place) ---` (mirror registry.py "one editable place" pattern). Inline comment per column documents its source field, especially `inp_proxy_tbt_ms` ("# D-11/D-15: labeled column name — never a bare 'inp' header").

    Helper `_metric_sample_median(ms: MetricSample | None) -> float | None`: returns `ms.median if ms is not None else None`. Used to flatten MetricSample fields for the CSV row.

    Helper `_build_csv_row(run: RunRecord, page: PageResult) -> dict[str, str]`: returns a dict keyed by `CSV_COLUMNS` with each value coerced to a string (or empty string for None). Special cases:
    - `"page": ""` (Phase 2 single-URL; Phase 3 will fill from `<title>` per Open Q2)
    - `"url": page.url`
    - `"test_date": run.started_at.isoformat()`
    - `"cache_disabled": "TRUE"` (RUN-03 cold cache is the always-on invariant per Phase 2)
    - `"total_page_load_time"`: per Open Q4 (RESOLVED) — taken from `PageResult.diagnostics["interactive"]["numericValue"]` (LH's TTI numericValue, ms). MEDIUM-4 fix: the normalizer (02-01 Task 3) carves `interactive` out of D-12 and includes it in `diagnostics` UNCONDITIONALLY (regardless of audit `score`), so this lookup never goes missing on a healthy page. The only case where the key is absent is a D-13 non-2xx response (where LH skips category-level audits entirely) — in that case, write empty string. Document the lookup path in a comment.
    - `"request_count": str(page.request_count) if page.request_count is not None else ""` — and so on for every scalar field.
    - MetricSample fields use `_metric_sample_median(page.lcp_ms)`, etc.
    - `"run_id": str(run.id)` (UUID → str).
    - `"schema_version": str(run.schema_version)`.

    Public function `write_outputs(run_record: RunRecord, *, output_dir: Path, raw_lh_json: str | None = None, raw_lh_html: str | None = None) -> Path`:
    - Returns the per-run directory path (`<output_dir>/<run_id>/`) for the caller's hint message.
    - `run_dir = Path(output_dir) / str(run_record.id)`
    - `run_dir.mkdir(parents=True, exist_ok=True)`
    - Write `result.json` atomically via `tempfile.NamedTemporaryFile(dir=run_dir, delete=False, mode='w', suffix='.tmp.json')` + `os.replace(tmp.name, run_dir / 'result.json')` (mirrors store.py atomic-write — file-I/O analog of `with conn:` transaction).
    - Content: `run_record.model_dump_json(indent=2)`.
    - Write `result.csv` similarly via `csv.DictWriter` with `fieldnames=CSV_COLUMNS` + `writer.writeheader()` then one `writer.writerow(_build_csv_row(run_record, page))` per page in `run_record.pages`.
    - For each page in `run_record.pages`:
      - If `raw_lh_json is not None` OR `raw_lh_html is not None`:
        - `lh_dir = run_dir / "lighthouse"`; `lh_dir.mkdir(parents=True, exist_ok=True)`
        - `slug = page_slug(page.url_key)` (IN-02 BOUNDARY — never use page.url_key directly as a path component)
        - Write `lh_dir / f"{slug}.json"` with the raw_lh_json content if provided
        - Write `lh_dir / f"{slug}.html"` with the raw_lh_html content if provided
      - Collision suffix: if a `<slug>.json` already exists (e.g. two pages map to the same slug), append `__1`, `__2`, … per D-07 (Claude's discretion: planner picks `__N` convention per CONTEXT). Phase 2 single-URL means collision shouldn't happen; the suffix mechanism is for Phase 3.
    - Return `run_dir`.

    Critical invariants:
    - EVERY filesystem path component derived from a URL goes through `page_slug()`. Never `f"{page.url_key}.json"`. The grep guard in `<verify>` enforces this.
    - The CSV column header for the TBT field is `inp_proxy_tbt_ms` — the field NAME itself is the labeling (D-11/D-15). NEVER a bare `inp` header. The Rich human table in Task 2 uses `INP_PROXY_DISPLAY_LABEL` for the display label; the CSV uses the field name.
    - All file writes are atomic via `tempfile.NamedTemporaryFile` + `os.replace` so a crash mid-write doesn't leave a half-CSV that downstream tooling would silently mis-parse.

    Create `tests/test_output.py` per 02-PATTERNS § "tests/test_output.py":
    - Imports: `import csv, json, pytest`, `from pathlib import Path`, `from perfcrawl.models import RunRecord, PageResult, MetricSample`, `from perfcrawl.output import write_outputs, CSV_COLUMNS`.
    - Reuse the `sample_run` fixture from conftest.py (Phase 1 — already has the full v2 shape with two pages).
    - One named test per behavior bullet. Test the IN-02 traversal-vector case by constructing a PageResult with `url_key="https://x.com/a/../b"` (LITERAL `..`) and asserting the on-disk artifact stem has no `..`.
    - Verify atomic-write doesn't leave `.tmp` files: after `write_outputs`, assert `list(run_dir.glob("*.tmp*"))` is empty.

    RED → GREEN:
    - First commit: write `tests/test_output.py`, run `uv run pytest tests/test_output.py -x`, confirm `ImportError`. Commit `test(02-04): RED — failing output writer tests`.
    - Second commit: implement `src/perfcrawl/output.py`. Run the same test, confirm green. Commit `feat(02-04): CSV + JSON + raw-LH writers with IN-02-safe slug + locked CSV_COLUMNS`.
  </action>
  <verify>
    <automated>cd /Users/sneaky/JKL101/performance-statistics-gathering && uv run pytest tests/test_output.py -x && uv run python -c "
from pathlib import Path
import tempfile, json
from perfcrawl.output import write_outputs, CSV_COLUMNS
from tests.conftest import sample_run as _sample_run_fixture  # noqa
# Build the fixture inline:
from datetime import datetime, UTC
from uuid import UUID
from perfcrawl.models import RunRecord, PageResult, MetricSample
sr = RunRecord(id=UUID('3f1c2b9a-0000-4000-8000-0000000000c3'), started_at=datetime(2026,5,25,12,0,0,tzinfo=UTC), target='https://x/', pages=[PageResult(url='https://x/', url_key='https://x/', perf_score=80.0, lcp_ms=MetricSample(median=2400.0, samples=[2300.0,2400.0,2500.0]))])
with tempfile.TemporaryDirectory() as td:
    run_dir = write_outputs(sr, output_dir=Path(td))
    assert (run_dir / 'result.json').exists(), 'result.json missing'
    assert (run_dir / 'result.csv').exists(), 'result.csv missing'
    loaded = RunRecord.model_validate_json((run_dir / 'result.json').read_text())
    assert loaded.model_dump() == sr.model_dump(), 'JSON round-trip failed'
    with open(run_dir / 'result.csv') as f:
        rows = list(csv.reader(f))
    assert rows[0] == CSV_COLUMNS, f'CSV header drift: {rows[0]}'
    print('output OK', run_dir)
" </automated>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && python3 -c "import re; src = open('src/perfcrawl/output.py').read(); assert 'page_slug' in src, 'output.py must call page_slug (IN-02 boundary)'; bad = re.findall(r'f[\"\\']{[^}]*url_key[^}]*}\\.(?:json|html)', src); assert bad == [], f'raw url_key used in filename: {bad}'; print('IN-02 boundary OK')"</grep>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && grep -c "inp_proxy_tbt_ms" src/perfcrawl/output.py</grep>
  </verify>
  <done>
    - `src/perfcrawl/output.py` exists exporting `write_outputs(run_record, *, output_dir, raw_lh_json=None, raw_lh_html=None) -> Path` and the locked `CSV_COLUMNS` list.
    - `tests/test_output.py` has ≥7 named tests; all green.
    - JSON round-trip identity holds (Phase 1 hybrid-store contract analog).
    - CSV header equals `CSV_COLUMNS` exactly.
    - IN-02 grep guard passes: `output.py` calls `page_slug` AND has no `f"{url_key}.json"`-style raw-key-as-path pattern.
    - `inp_proxy_tbt_ms` appears in `output.py` (the labeled column name); no bare `inp` column header.
    - Two commits: `test(02-04): RED — failing output writer tests` then `feat(02-04): CSV + JSON + raw-LH writers with IN-02-safe slug + locked CSV_COLUMNS`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cli.py — Typer entry point + Rich table + --json + persistence + exit codes</name>
  <files>src/perfcrawl/cli.py, tests/test_cli.py, pyproject.toml, .gitignore, uv.lock</files>
  <read_first>
    - .planning/phases/02-single-page-measurement-slice/02-PATTERNS.md § "src/perfcrawl/cli.py" lines 509-566 (the Typer command structure + UserError/MeasurementError exception arms + `--json` stdout-write + Rich table on default) and § "src/perfcrawl/cli.py — entry point wiring" lines 570-592 (pyproject.toml [project.scripts] + deps additions)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § "Typer CLI shape" lines 806-845 (verbatim Typer command structure + Console(stderr=True) for progress)
    - .planning/phases/02-single-page-measurement-slice/02-CONTEXT.md § D-05 (verb `measure`), D-06 (human Rich table on stdout default + `--json` machine output + stderr for progress), D-08 (median displayed with `(median of N)` footer), D-15 (exit codes 0/1/2)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § Open Q5 (documented setup not auto-`npm ci`)
    - src/perfcrawl/orchestrator.py (from 02-03 — `measure_url`, `MeasurementError`, `UserError` — the public API this CLI sits on top of)
    - src/perfcrawl/output.py (from Task 1 above — `write_outputs`)
    - src/perfcrawl/store.py (Phase 1 — `init_db(conn)` + `write_run(conn, run_record)`; the SQLite store this CLI persists to)
    - src/perfcrawl/constants.py (DEFAULT_SAMPLES_N, INP_PROXY_DISPLAY_LABEL, ExitCode — never inline)
    - .planning/phases/01-data-model-persistence-foundation/01-LEARNINGS.md § "Phase 1 is library-only" (the `[project.scripts]` entry was deliberately removed in commit 5aa4222; Phase 2 restores it pointing at the real CLI)
  </read_first>
  <behavior>
    - `test_help_runs` (CLI-01 sanity): `runner.invoke(app, ["--help"])` exits 0 and the help text contains `"measure"` and `"--samples"` and `"--json"`.
    - `test_measure_help_runs`: `runner.invoke(app, ["measure", "--help"])` exits 0; help text mentions URL argument, `--samples`, `--emulation`, `--json`, `--output-dir`.
    - `test_exit_zero_on_success` (D-15 + CLI-01): monkeypatch `perfcrawl.cli.measure_url` (the module-level reference) to return a stub `RunRecord`. monkeypatch `perfcrawl.cli.write_outputs` to a no-op. monkeypatch `perfcrawl.cli._persist_run` (or `store.write_run`) to a no-op. Invoke `["measure", "https://example.com", "--output-dir", str(tmp_path)]`. `result.exit_code == 0`.
    - `test_exit_one_on_user_error` (D-15): monkeypatch `measure_url` to raise `UserError("URL is empty")`. Invoke `["measure", ""]`. `result.exit_code == 1`. `result.stderr` (or `result.stdout` if mix_stderr=False is supported here) contains an error message citing the empty URL.
    - `test_exit_two_on_measurement_error` (D-15 + D-14): monkeypatch `measure_url` to raise `MeasurementError("all 3 samples failed")`. Invoke `["measure", "https://example.com"]`. `result.exit_code == 2`.
    - `test_json_flag_emits_valid_json_on_stdout` (D-06 + CLI-01): monkeypatch `measure_url` to return a stub RunRecord. Invoke `["measure", "https://example.com", "--json", "--output-dir", str(tmp_path)]`. `result.exit_code == 0`. `json.loads(result.stdout)` succeeds; the parsed dict has keys `"id"`, `"pages"`, `"started_at"`, `"schema_version"`. (Use `CliRunner(mix_stderr=False)` so stderr doesn't contaminate stdout in the captured output — the production code already routes errors via `Console(stderr=True)`.)
    - `test_no_json_flag_emits_rich_table_on_stdout`: monkeypatch `measure_url` to return a stub. Invoke without `--json`. `result.stdout` is NOT valid JSON (`json.loads` raises) — it contains human-readable column labels like `"INP (lab proxy, TBT-based)"` (the constants.INP_PROXY_DISPLAY_LABEL value — assert this exact substring is present).
    - `test_inp_label_visible_in_rich_table` (D-11 + D-15 + labeled-proxy invariant): the Rich table renders the constants.INP_PROXY_DISPLAY_LABEL string verbatim. The displayed value MUST be the median TBT from the PageResult's `inp_proxy_tbt_ms.median` (not a bare `inp` field — defense-in-depth above the model-layer floor).
    - `test_progress_to_stderr` (D-06): when running interactively (no `--json`), progress lines like "Sample 1 of 3 …" go to `result.stderr`, never to `result.stdout`. Default behavior: stdout is exclusively for the final result, stderr for progress + errors. (Implementation note: use `rich.console.Console(stderr=True)` for progress per RESEARCH Pattern.)
    - `test_persistence_writes_to_sqlite` (HIST-01 reuse): after a successful measure, the SQLite file (default `perfcrawl.db` or `<output_dir>/perfcrawl.db` — Claude's discretion; pick the simpler `output_dir / "perfcrawl.db"` so the DB lives with its artifacts) contains a row in `runs` whose `id` equals the stub run's id. Use `tmp_path` for the output dir.
    - `test_on_disk_layout` (D-07 / OUT-03 / OUT-04): after a successful measure, `tmp_path/<run_id>/result.json` AND `tmp_path/<run_id>/result.csv` exist. The Rich-table case may or may not write `lighthouse/<slug>.{json,html}` depending on whether the orchestrator stub passes raw LH data through (in this plan: Phase 2 single-URL with single-sample, orchestrator returns raw LH JSON via `RunRecord.pages[0].diagnostics` OR via an auxiliary return value — Claude's discretion: the orchestrator needs to expose `raw_lh_json` + `raw_lh_html` somehow; recommended approach below).
    - `test_exit_one_when_output_dir_unwriteable` (D-15): when `output_dir` is a file (not a dir), `write_outputs` raises `OSError`/`NotADirectoryError`; the CLI maps this to `ExitCode.USER_ERROR == 1`. Use a tmp_path-based file as the "directory" argument.
    - `test_no_args_shows_help`: `runner.invoke(app, [])` shows help and exits non-zero (Typer's default for `no_args_is_help=True`).
  </behavior>
  <action>
    Add CLI dependencies to `pyproject.toml`:
    ```
    [project]
    dependencies = [
        "pydantic>=2.10,<3",
        "playwright>=1.60,<2",
        "rich>=13",
        "typer>=0.15",
        "w3lib>=2.3,<3",
    ]

    [project.scripts]
    perfcrawl = "perfcrawl.cli:app"
    ```
    (Alphabetical order maintained; rich + typer inserted before w3lib.) Restore the `[project.scripts]` block — Phase 1 deliberately removed it per 01-LEARNINGS (commit 5aa4222); Phase 2 brings it back pointing at the real CLI. Run `uv lock` + `uv sync`. Reaffirm: no `lighthouse` entry on the PyPI side (Pitfall 8 guard from 02-03 still active). MEDIUM-3 (plan-check fix): MUST preserve the `playwright>=1.60,<2` entry added in 02-03 — add `typer` + `rich` ALONGSIDE the existing deps, never replace the dependency array wholesale.

    Add `output/` to `.gitignore` (per 02-CONTEXT Claude's discretion — recommended YES per D-07 + RESEARCH § "Sensitive URL leakage in committed output/" Security Domain entry). Add ALSO `perfcrawl.db` and `*.sqlite` if not already present (Phase 1 may have done these; check before duplicating).

    Create `src/perfcrawl/cli.py` per 02-PATTERNS § "src/perfcrawl/cli.py":

    Module docstring opens: "perfcrawl measure — single-URL end-to-end audit (Phase 2 D-05/D-06/D-15).\n\nThe CLI is the controller layer: parse argv → build a RunConfig → hand off to orchestrator.measure_url() → write outputs to disk → persist to SQLite → render result (Rich table to stdout for humans, RunRecord JSON to stdout for `--json`). Progress and errors always go to stderr so machine-readable stdout under `--json` is never contaminated (D-06).\n\nExit codes (D-15):\n  0  page measured (including non-2xx per D-13 partial)\n  1  user error — bad URL, bad flags, can't write output dir\n  2  measurement error — all N samples failed, Chrome won't launch, etc.\n(Phase 6 BUDG-01 will carve out 10+; the gap is intentional.)"

    Imports: `import sqlite3, sys`, `from pathlib import Path`, `import typer`, `from rich.console import Console`, `from rich.table import Table`. Plus `from perfcrawl.orchestrator import measure_url, MeasurementError, UserError`, `from perfcrawl.output import write_outputs`, `from perfcrawl.store import init_db, write_run`, `from perfcrawl.constants import DEFAULT_SAMPLES_N, INP_PROXY_DISPLAY_LABEL, ExitCode`.

    Module-level: `app = typer.Typer(no_args_is_help=True, add_completion=False)`. `err_console = Console(stderr=True)` (per D-06 — progress + errors → stderr). `out_console = Console()` (stdout for human Rich table).

    Note on the orchestrator return shape: the orchestrator (02-03) locks its public API as `measure_url(...) -> tuple[RunRecord, dict[str, tuple[str, str]]]` (the second element maps `page.url_key → (reportJson, reportHtml)` from the first successful sample). 02-04 destructures this tuple — no back-edit to `orchestrator.py` happens in this plan. This is the HIGH-1 fix from plan-check: the raw-artifact side-channel was moved into 02-03's contract so it's not a hidden modification in 02-04.

    Subcommand `measure`:
    ```
    @app.command()
    def measure(
        url: str = typer.Argument(..., help="URL to audit"),
        samples: int = typer.Option(DEFAULT_SAMPLES_N, "--samples", "-n", min=1),
        emulation: str = typer.Option("mobile", "--emulation", help="mobile | desktop"),
        output_json: bool = typer.Option(False, "--json", help="Emit machine-readable JSON to stdout"),
        output_dir: Path = typer.Option(Path("output"), "--output-dir"),
    ) -> None:
        try:
            run_record, raw_artifacts = measure_url(url=url, samples=samples, emulation=emulation)
        except UserError as e:
            err_console.print(f"[red]error:[/red] {e}", style="bold")
            raise typer.Exit(code=int(ExitCode.USER_ERROR))
        except MeasurementError as e:
            err_console.print(f"[red]measurement failed:[/red] {e}", style="bold")
            raise typer.Exit(code=int(ExitCode.MEASUREMENT_ERROR))

        # Write outputs (OUT-03 / OUT-04). Catch OS errors as USER_ERROR per D-15.
        try:
            first_page = run_record.pages[0]
            raw_json, raw_html = raw_artifacts.get(first_page.url_key, (None, None))
            run_dir = write_outputs(run_record, output_dir=output_dir, raw_lh_json=raw_json, raw_lh_html=raw_html)
        except OSError as e:
            err_console.print(f"[red]error:[/red] cannot write to {output_dir}: {e}")
            raise typer.Exit(code=int(ExitCode.USER_ERROR))

        # Persist to SQLite (HIST-01). DB lives alongside the artifacts.
        db_path = output_dir / "perfcrawl.db"
        conn = sqlite3.connect(db_path)
        try:
            init_db(conn)  # idempotent per Phase 1 contract
            write_run(conn, run_record)
        finally:
            conn.close()

        # Render the final result on stdout (D-06).
        if output_json:
            sys.stdout.write(run_record.model_dump_json(indent=2))
            sys.stdout.write("\n")
        else:
            _render_human_table(run_record, samples=samples, run_dir=run_dir)
        # Implicit exit 0 (D-13: success-or-partial).
    ```

    Helper `_render_human_table(run: RunRecord, *, samples: int, run_dir: Path) -> None`:
    - Build a Rich `Table(title=f"perfcrawl: {run.target}")` with two columns: `"Metric"`, `"Value"`.
    - Rows per metric for the first page (Phase 2 is single-URL):
      - `"Performance"`, `f"{page.perf_score:.0f}" if page.perf_score is not None else "-"`
      - `"Accessibility"`, `…`
      - `"SEO"`, `…`
      - `"Best Practices"`, `…`
      - `"LCP (ms)"`, `f"{page.lcp_ms.median:.0f}"` if `page.lcp_ms and page.lcp_ms.median is not None else "-"`
      - `"CLS"`, `f"{page.cls.median:.3f}"` if available
      - `INP_PROXY_DISPLAY_LABEL`, `f"{page.inp_proxy_tbt_ms.median:.0f} ms"` (THE LABELED ROW — uses the constants string verbatim per D-11)
      - `"TTFB (ms)"`, `f"{page.ttfb_ms.median:.0f}"` if available
      - `"Requests"`, `str(page.request_count) if … else "-"`
      - `"Total bytes"`, `…`
      - `"Slowest request"`, `f"{page.slowest_request_url} ({page.slowest_request_ms:.0f} ms)"` if both not None else `"-"`
      - `"Status code"`, `str(page.status_code) if … else "-"`
    - Caption: `f"(median of {samples}) · written to {run_dir}"` (D-08 footer pattern).
    - `out_console.print(table)` — to STDOUT (not stderr).

    Critical invariants (per 02-PATTERNS labeled-proxy invariant):
    - The Rich table's INP row label is `INP_PROXY_DISPLAY_LABEL` (the constants.py string). NEVER a hand-coded `"INP"` or `"INP (ms)"`. Reading the constant guarantees consistency with the model layer's `_no_bare_inp` floor + the CSV `inp_proxy_tbt_ms` column header.
    - The Rich table value for that row is `page.inp_proxy_tbt_ms.median` — NEVER `page.inp` (which doesn't exist) or `page.tbt_ms` (no such field). The variable name `inp_proxy_tbt_ms` is the only path.
    - Defense-in-depth grep: as in 02-01 Task 3, a meta-test in `test_cli.py` reads `inspect.getsource(perfcrawl.cli)` and asserts no `\binp\b` token outside `inp_proxy_tbt_ms` substring AND `INP_PROXY_DISPLAY_LABEL` appears at least once.

    Create `tests/test_cli.py` per 02-PATTERNS § "tests/test_cli.py":
    - `from typer.testing import CliRunner` and `runner = CliRunner(mix_stderr=False)` (Typer/Click split stdout/stderr — needed for the JSON-on-stdout test).
    - Helper `_make_stub_run() -> RunRecord` builds a minimal valid RunRecord with one page (UUID and started_at filled).
    - Helper `_make_stub_artifacts() -> dict[str, tuple[str, str]]` returns `{url_key: ('{"lhr":{}}', '<html/>')}` for the orchestrator stub.
    - One named test per behavior bullet. Mock `perfcrawl.cli.measure_url` (the module-level reference) so the CLI tests don't spawn real Chrome/Node.

    RED → GREEN:
    - First commit: write `tests/test_cli.py`, run `uv run pytest tests/test_cli.py -x`, confirm `ImportError: cannot import name 'app' from 'perfcrawl.cli'`. Commit `test(02-04): RED — failing Typer CLI tests`.
    - Second commit: implement `src/perfcrawl/cli.py`, update `pyproject.toml` (deps + [project.scripts]), update `.gitignore` (output/). No edits to `orchestrator.py` or `test_orchestrator.py` — 02-03 already locked the tuple return contract; 02-04 only destructures it. Run `uv run pytest`, confirm green. Commit `feat(02-04): Typer CLI + Rich human table + --json + SQLite persistence + exit codes`.
  </action>
  <verify>
    <automated>cd /Users/sneaky/JKL101/performance-statistics-gathering && uv run pytest tests/test_cli.py -x && uv run pytest -x && uv run python -c "from perfcrawl.cli import app; from typer.testing import CliRunner; r = CliRunner(mix_stderr=False).invoke(app, ['--help']); assert r.exit_code == 0, r.stdout; assert 'measure' in r.stdout; print('CLI help OK')" && uv run python -c "import inspect, re; src = inspect.getsource(__import__('perfcrawl.cli', fromlist=['_x'])); assert 'INP_PROXY_DISPLAY_LABEL' in src, 'CLI must use INP_PROXY_DISPLAY_LABEL constant'; bare = [m for m in re.findall(r'\binp\b(?!_proxy)', src)]; assert bare == [], f'bare-INP in cli.py: {bare}'; print('labeling invariant OK')"</automated>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && grep -c "perfcrawl.cli:app" pyproject.toml && grep -c "^output/$" .gitignore</grep>
    <grep>cd /Users/sneaky/JKL101/performance-statistics-gathering && ! grep -nE "^[[:space:]]*[\"']lighthouse[\"']" pyproject.toml 2>/dev/null | grep -v '^[[:space:]]*#' | grep .</grep>
  </verify>
  <done>
    - `src/perfcrawl/cli.py` exists exporting `app` (Typer) and `measure` (the subcommand).
    - `pyproject.toml` lists `typer>=0.15` and `rich>=13` under deps; `[project.scripts] perfcrawl = "perfcrawl.cli:app"` block exists; no `lighthouse` PyPI entry (Pitfall 8 reaffirmed).
    - `.gitignore` contains `output/`.
    - `tests/test_cli.py` has ≥10 named tests; all green.
    - `uv run pytest` (full suite Phase 1 + Phase 2-01..04) green.
    - Two grep guards pass: `[project.scripts]` points at `perfcrawl.cli:app`; `INP_PROXY_DISPLAY_LABEL` is used (labeled-proxy defense-in-depth).
    - Two commits: `test(02-04): RED — failing Typer CLI tests` then `feat(02-04): Typer CLI + Rich human table + --json + SQLite persistence + exit codes`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end smoke test (optional, gated by pytest -m e2e marker)</name>
  <files>tests/test_e2e.py</files>
  <read_first>
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § Validation Architecture > Test Framework lines 957-1011 (the e2e marker convention) and § Phase Requirements → Test Map last row "E2E smoke" — `pytest -m e2e tests/test_e2e.py -x`
    - .planning/phases/02-single-page-measurement-slice/02-VALIDATION.md § "Wave 0 Requirements" last bullet (`tests/test_e2e.py` — optional, `@pytest.mark.e2e`)
    - .planning/phases/02-single-page-measurement-slice/02-RESEARCH.md § Environment Availability (Node 22+ + Chromium needed; the `uv run playwright install chromium` step from 02-03)
    - tests/conftest.py (existing e2e marker registration from 02-01 Task 2)
    - src/perfcrawl/cli.py (Task 2 above — the CLI under test)
  </read_first>
  <behavior>
    - `test_e2e_measure_example_com` (gated by `@pytest.mark.e2e`): runs the real CLI against `https://example.com/` with `--samples 1 --json --output-dir <tmp_path>`. Requires: Node 22+ installed, `lighthouse-worker/node_modules` populated (`npm ci` run), Playwright Chromium installed (`uv run playwright install chromium`). Excluded by default; included only with `uv run pytest -m e2e`.
    - Assertion 1: subprocess exit code 0.
    - Assertion 2: `<tmp_path>/<run_id>/result.json` exists and parses as RunRecord.
    - Assertion 3: the parsed RunRecord has one page; the page has `perf_score is not None`, `lcp_ms.median is not None`, `inp_proxy_tbt_ms.median is not None`, `chrome_version is not None`, `lighthouse_version` starting with `"13."`.
    - Assertion 4: `<tmp_path>/<run_id>/lighthouse/<slug>.json` and `.html` exist.
    - Assertion 5: `<tmp_path>/perfcrawl.db` exists and contains one row in `runs`.
  </behavior>
  <action>
    Create `tests/test_e2e.py`:
    ```python
    """End-to-end smoke (Phase 2 RESEARCH § Validation Architecture E2E row).

    Gated by `@pytest.mark.e2e`; excluded by default. Run with:

        uv run pytest -m e2e tests/test_e2e.py -x

    Requires: Node >=22.19, `cd lighthouse-worker && npm ci`,
              `uv run playwright install chromium`.
    """

    import json
    import subprocess
    import sqlite3
    from pathlib import Path
    from uuid import UUID

    import pytest

    from perfcrawl.models import RunRecord

    pytestmark = pytest.mark.e2e


    def test_e2e_measure_example_com(tmp_path: Path):
        result = subprocess.run(
            [
                "uv", "run", "perfcrawl", "measure",
                "https://example.com/",
                "--samples", "1",
                "--json",
                "--output-dir", str(tmp_path),
            ],
            capture_output=True,
            text=True,
            timeout=120,
        )
        assert result.returncode == 0, f"exit {result.returncode}; stderr={result.stderr}; stdout={result.stdout[:500]}"

        parsed = json.loads(result.stdout)
        run_record = RunRecord.model_validate(parsed)
        assert len(run_record.pages) == 1
        page = run_record.pages[0]
        assert page.perf_score is not None
        assert page.lcp_ms is not None and page.lcp_ms.median is not None
        assert page.inp_proxy_tbt_ms is not None  # D-11 — labeled proxy
        assert run_record.chrome_version is not None
        assert run_record.lighthouse_version is not None
        assert run_record.lighthouse_version.startswith("13.")

        run_dir = tmp_path / str(run_record.id)
        assert (run_dir / "result.json").exists()
        assert (run_dir / "result.csv").exists()
        lh_dir = run_dir / "lighthouse"
        assert lh_dir.exists()
        assert any(p.suffix == ".json" for p in lh_dir.iterdir())
        assert any(p.suffix == ".html" for p in lh_dir.iterdir())

        db = tmp_path / "perfcrawl.db"
        assert db.exists()
        conn = sqlite3.connect(db)
        try:
            rows = conn.execute("SELECT id FROM runs WHERE id = ?", (str(run_record.id),)).fetchall()
            assert len(rows) == 1
        finally:
            conn.close()
    ```

    Confirm the e2e marker is registered (from 02-01 Task 2). If `uv run pytest --markers` does NOT show the `e2e` marker, add it to `pyproject.toml` `[tool.pytest.ini_options]` markers list.

    Single commit (this test starts as `MISSING — Wave 0 must create tests/test_e2e.py first` per the planner Nyquist rule, then it's CREATED here): `test(02-04): e2e smoke test for measure command (gated by pytest -m e2e)`.

    Note: Do NOT run this test as part of the standard `uv run pytest` invocation. It's documented in README + run by the developer before `/gsd-verify-work` per 02-VALIDATION.md § Sampling Rate "Before /gsd-verify-work: full suite green AND one-shot manual e2e".
  </action>
  <verify>
    <automated>cd /Users/sneaky/JKL101/performance-statistics-gathering && uv run pytest --collect-only tests/test_e2e.py 2>&1 | grep -E "test_e2e_measure_example_com|e2e" | head -5 && uv run pytest -x 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `tests/test_e2e.py` exists with `pytestmark = pytest.mark.e2e` so it is excluded from default `pytest` runs.
    - `uv run pytest --collect-only tests/test_e2e.py` shows the test is collected under the e2e marker.
    - `uv run pytest -x` (default, no `-m e2e`) does NOT run `test_e2e_measure_example_com` (its execution requires Node + Chromium + network — leave that to the developer's pre-`/gsd-verify-work` manual smoke).
    - One commit: `test(02-04): e2e smoke test for measure command (gated by pytest -m e2e)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User argv → Typer parser | Bad URLs / flags must map to ExitCode.USER_ERROR, not crash with a stack trace |
| Orchestrator → on-disk artifacts | The IN-02 slug boundary (page_slug) is re-applied here so the CLI cannot accidentally bypass it |
| Output dir → host filesystem | `output_dir` is a user-supplied path; writing outside the directory the user named is a path-confusion bug |
| SQLite store → on-disk DB | The Phase 1 store's atomic-write contract is preserved; the CLI uses the existing `init_db` + `write_run` API unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-PATH | Tampering | `output.py::write_outputs` (URL → filesystem path) | mitigate | Every per-page artifact path goes through `page_slug(page.url_key)` (the Phase 2-01 IN-02 boundary). Grep guard in Task 1 verify asserts no `f"{url_key}.json"` pattern; `test_slug_in_artifact_path_never_traverses` exercises the literal-`..` IN-02 vector |
| T-02-04-DISCLOSURE | Disclosure | Committed `output/` containing measured URLs + HTML reports | mitigate | `output/` added to `.gitignore` (Claude's discretion per 02-CONTEXT — recommended YES per RESEARCH § Security Domain "Sensitive URL leakage in committed output/"). Documented in README that artifact dir is per-developer, never committed |
| T-02-04-LABEL | Spoofing | Rich table INP labeling drift | mitigate | The Rich table row label reads `INP_PROXY_DISPLAY_LABEL` from constants.py (never a hand-coded `"INP"`), and the value comes from `page.inp_proxy_tbt_ms.median` (never a `page.inp` that doesn't exist). The model-layer `_no_bare_inp` validator + normalizer grep guard (02-01 Task 3) + CSV column-name `inp_proxy_tbt_ms` (Task 1) + this Rich label form a 4-layer defense-in-depth. Meta-test asserts `\binp\b` token absent in cli.py source |
| T-02-04-SLOPSQUAT | Tampering | `pyproject.toml` (Pitfall 8 reaffirmation, 3rd time in Phase 2) | mitigate | Phase 2-04 adds `typer>=0.15` (tiangolo, mature, RESEARCH Package Legitimacy Audit verified) and `rich>=13` (Textualize, mature, verified). Grep guard reaffirms `pyproject.toml` lighthouse-line count == 0 |
| T-02-04-WRITE | Denial of Service | Output dir unwriteable (full disk, perm denied) | mitigate | `write_outputs` propagates `OSError`; CLI catches as `ExitCode.USER_ERROR` per D-15. Test `test_exit_one_when_output_dir_unwriteable` exercises this |
| T-02-04-DB | Tampering | SQLite write path | mitigate | Reuses Phase 1 `store.write_run` unchanged — atomic `with conn:` transaction, parameterized SQL, `PRAGMA foreign_keys=ON` per-connection. No new query construction in Phase 2-04 |

ASVS coverage: V5 (Input Validation — URL+flags via Typer + UserError arm), V7 (Error Handling — three-way exit-code mapping per D-15, no stack-trace leakage), V8 (Data Protection — `output/` in .gitignore prevents URL/HTML leakage), V12 (Files and Resources — IN-02 slug boundary applied at output writers, atomic write to prevent half-CSV), V14 (Configuration — typer/rich versions pinned in pyproject.toml).
</threat_model>

<verification>
```bash
cd /Users/sneaky/JKL101/performance-statistics-gathering
# Full suite green (Phase 1 + Phase 2-01..04, excluding e2e):
uv run pytest -x
# CLI help works:
uv run perfcrawl --help
uv run perfcrawl measure --help
# Smoke test the full pipeline (manual; gated on Node + Chromium installed):
# uv run perfcrawl measure https://example.com/ --samples 1 --json --output-dir /tmp/perfcrawl-smoke
# uv run pytest -m e2e tests/test_e2e.py -x
```
</verification>

<success_criteria>
- `src/perfcrawl/output.py` and `src/perfcrawl/cli.py` exist with their documented public APIs.
- `pyproject.toml` lists `playwright`, `rich`, `typer` under runtime deps; `[project.scripts] perfcrawl = "perfcrawl.cli:app"` block present; no PyPI `lighthouse` (Pitfall 8 reaffirmed).
- `.gitignore` contains `output/`.
- `tests/test_output.py` ≥7 tests, `tests/test_cli.py` ≥10 tests, `tests/test_e2e.py` ≥1 e2e-marked test — all default-suite tests green.
- `uv run pytest` (full suite Phase 1 + Phase 2-01..04, no e2e) green.
- All Phase 2 requirements (METRIC-01..05, RUN-01..04, OUT-03, OUT-04, CLI-01) have at least one passing automated test referencing their behavior.
- D-05 (subcommand verb `measure`), D-06 (Rich default + `--json` opt-in + stderr for progress), D-07 (on-disk layout), D-08 (median display + footer), D-13 (success exit 0 on non-2xx), D-14 (MeasurementError on all-fail), D-15 (exit 0/1/2 mapping) are each cited by name in code or test docstrings.
- A developer with Node 22+, `cd lighthouse-worker && npm ci` run, and `uv run playwright install chromium` run, can type `uv run perfcrawl measure https://example.com/ --samples 1 --json` and get a valid RunRecord JSON on stdout, exit code 0.
</success_criteria>

<output>
Create `.planning/phases/02-single-page-measurement-slice/02-04-SUMMARY.md` when done.
</output>
