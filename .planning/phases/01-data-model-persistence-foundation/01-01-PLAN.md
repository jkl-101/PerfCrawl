---
phase: 01-data-model-persistence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/perfcrawl/__init__.py
  - src/perfcrawl/registry.py
  - src/perfcrawl/canonical.py
  - tests/test_canonical.py
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "uv-managed project exists with src/perfcrawl/ package and pytest+ruff configured"
    - "Tracking params, fragment, and trailing slash are dropped; remaining query params are sorted; scheme+host lowercased"
    - "Two URL variants of the same logical page collapse to one canonical key"
    - "Genuinely distinct pages (?page=2 vs ?page=3, www vs apex) keep distinct keys"
    - "A malformed/non-URL string does not crash canonical_key()"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv project config with deps, [tool.pytest.ini_options], [tool.ruff]"
      contains: "tool.pytest.ini_options"
    - path: "src/perfcrawl/registry.py"
      provides: "TRACKING_PARAM_DENYLIST (D-04) and METRIC_POLARITY + Polarity enum (D-09) — the two one-editable-place tables"
      contains: "TRACKING_PARAM_DENYLIST"
    - path: "src/perfcrawl/canonical.py"
      provides: "canonical_key(url) — w3lib wrapper + trailing-slash rule"
      contains: "def canonical_key"
    - path: "tests/test_canonical.py"
      provides: "D-02..D-05 canonicalization assertions incl. collapse + no-over-merge + malformed input"
      contains: "test_variants_collapse"
  key_links:
    - from: "src/perfcrawl/canonical.py"
      to: "src/perfcrawl/registry.py"
      via: "imports TRACKING_PARAM_DENYLIST"
      pattern: "from perfcrawl.registry import TRACKING_PARAM_DENYLIST"
    - from: "src/perfcrawl/canonical.py"
      to: "w3lib.url"
      via: "canonicalize_url + url_query_cleaner"
      pattern: "from w3lib.url import"
---

<objective>
Scaffold the greenfield PerfCrawl Python project (uv + Ruff + pytest, src/perfcrawl/ package), establish the two "one editable place" registry tables (tracking-param denylist D-04, metric polarity D-09), and deliver the first capability slice end-to-end: canonical URL key derivation (success criterion #4, D-01..D-05) verified by pytest against fixtures.

Purpose: This is Wave 0 — nothing exists in the repo except CLAUDE.md and .planning/. Every later plan depends on this scaffold, the registry, and the canonical_key() function (Plan 02 uses it to set url_key on write; Plan 03 reads the polarity registry). Canonicalization is the only self-contained capability slice, so it ships first and proves the test harness works.
Output: A runnable uv project, the registry module, the canonical module, and a green test_canonical.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-model-persistence-foundation/01-CONTEXT.md
@.planning/phases/01-data-model-persistence-foundation/01-RESEARCH.md
@.planning/phases/01-data-model-persistence-foundation/01-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan CREATES that Plans 02 and 03 consume. No codebase exploration needed — these are the seams. -->

registry.py exports (the two one-editable-place tables):
  TRACKING_PARAM_DENYLIST: list[str]   # D-04 — denylist of query keys to drop
  class Polarity(StrEnum): LOWER_IS_BETTER = "lower"; HIGHER_IS_BETTER = "higher"
  METRIC_POLARITY: dict[str, Polarity]  # D-09 — maps metric field name -> Polarity

canonical.py exports:
  def canonical_key(url: str) -> str   # D-01..D-05 — derives the cross-run page-identity key

Recommended w3lib usage (from RESEARCH.md Code Examples — verify each D-02 sub-rule in tests, A2):
  - url_query_cleaner(url, TRACKING_PARAM_DENYLIST, remove=True, keep_fragments=False)  # drop tracking params (D-04)
  - canonicalize_url(cleaned, keep_fragments=False)  # RFC-3986 normalize, sort query, drop fragment (D-02/D-04/D-05)
  - then urlsplit/urlunsplit to strip trailing slash except root (D-03 — w3lib does NOT do this)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify w3lib dependency before install</name>
  <files>pyproject.toml (the w3lib dependency decision this gate governs)</files>
  <read_first>
    - .planning/phases/01-data-model-persistence-foundation/01-RESEARCH.md (§ Package Legitimacy Audit — w3lib is flagged [ASSUMED]; slopcheck unavailable this session)
    - CLAUDE.md (§ Technology Stack — confirms the locked stack; w3lib is a NEW research recommendation, not locked)
  </read_first>
  <what-built>
    Nothing automated yet — this is a supply-chain trust gate that MUST pass before any package install. w3lib 2.4.1 is a NEW external runtime dependency recommended by research for URL canonicalization (D-02/D-04/D-05). Researcher flagged it [ASSUMED] because slopcheck could not run offline this session. It is the canonicalization engine inside Scrapy, hosted under the official scrapy GitHub org, on PyPI since 2010. The documented zero-dep fallback is a ~50-line stdlib urllib.parse wrapper (RESEARCH.md Alternatives Considered).
  </what-built>
  <action>
    BLOCKING-HUMAN legitimacy gate — do not install w3lib until the user approves. Present the package facts (pypi.org/project/w3lib, latest 2.4.1, official scrapy org repo, provides w3lib.url.canonicalize_url + url_query_cleaner) and pause. Do NOT auto-approve regardless of workflow.auto_advance (threat T-01-SC). On "approved": Task 2 adds w3lib>=2.3,<3 as a runtime dep. On "use stdlib fallback": no w3lib dependency is added and canonical.py (Task 3) implements D-02..D-05 with stdlib urllib.parse only.
  </action>
  <how-to-verify>
    1. Open https://pypi.org/project/w3lib/ — confirm latest is 2.4.1, maintained by the Scrapy project, source repo github.com/scrapy/w3lib (official scrapy org).
    2. Confirm the package is the intended canonicalization library (provides w3lib.url.canonicalize_url and w3lib.url.url_query_cleaner).
    3. Decide: approve adding w3lib as a runtime dependency, OR instruct the executor to use the stdlib urllib.parse fallback wrapper documented in RESEARCH.md Alternatives Considered (then canonical.py implements D-02 sub-rules by hand and does NOT import w3lib).
  </how-to-verify>
  <verify>
    <human-check>User has explicitly typed "approved" or "use stdlib fallback" — execution does not proceed to Task 2 without one of these signals.</human-check>
  </verify>
  <acceptance_criteria>
    - User has typed "approved" (install w3lib) OR "use stdlib fallback" (no w3lib dependency).
    - This checkpoint is NEVER auto-approved regardless of workflow.auto_advance (legitimacy gate, threat T-01-SC).
  </acceptance_criteria>
  <done>The w3lib dependency decision is recorded (approved → install in Task 2, or stdlib fallback → no dep); install never happened before this approval.</done>
  <resume-signal>Type "approved" to add w3lib, or "use stdlib fallback" to canonicalize with stdlib only</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold uv project + registry tables</name>
  <files>pyproject.toml, uv.lock, src/perfcrawl/__init__.py, src/perfcrawl/registry.py</files>
  <read_first>
    - .planning/phases/01-data-model-persistence-foundation/01-RESEARCH.md (§ Installation, § Recommended Project Structure, § Code Examples registry.py, § Pattern 5 METRIC_POLARITY)
    - CLAUDE.md (§ Development Tools — uv 0.11.16, Ruff 0.15.14, pytest 9.0.3; § Recommended Stack — Pydantic 2.x)
    - .planning/phases/01-data-model-persistence-foundation/01-CONTEXT.md (D-04, D-09)
  </read_first>
  <action>
    Initialize the greenfield project with uv (install uv first if absent — verify the installer per RESEARCH.md Installation note; pip+venv is the documented fallback). Run `uv init --package perfcrawl` to create the pyproject.toml + src/perfcrawl/ layout. Add runtime deps `pydantic>=2.10,<3` and (if Task 1 approved w3lib) `w3lib>=2.3,<3`; add dev deps `pytest>=8,<10` and `ruff>=0.15,<0.16`. If Task 1 chose the stdlib fallback, do NOT add w3lib. Add `[tool.pytest.ini_options]` (testpaths = ["tests"], pythonpath/src layout configured so `import perfcrawl` resolves) and `[tool.ruff]` to pyproject.toml. Create the tests/ directory and tests/fixtures/ directory (empty placeholders are fine — fixtures are populated in Plan 02). Then create src/perfcrawl/registry.py holding the TWO one-editable-place tables: TRACKING_PARAM_DENYLIST (a list of query-param keys to drop — start with utm_source, utm_medium, utm_campaign, utm_term, utm_content, utm_id, gclid, fbclid, msclkid, mc_cid, mc_eid, _ga, ref, ref_src per D-04) and a Polarity StrEnum with members LOWER_IS_BETTER="lower" and HIGHER_IS_BETTER="higher" plus the METRIC_POLARITY dict mapping field names to Polarity (lower-is-better: lcp_ms, cls, inp_proxy_tbt_ms, ttfb_ms, total_bytes, request_count, slowest_request_ms; higher-is-better: perf_score, a11y_score, seo_score, best_practices_score) per D-09. Keep BOTH tables as plain module-level constants so later phases extend them without touching call sites. Do NOT scaffold any CLI/Typer, web, or measurement modules (out of scope — Phase 1 is library-only).
  </action>
  <verify>
    <automated>uv run python -c "from perfcrawl.registry import TRACKING_PARAM_DENYLIST, METRIC_POLARITY, Polarity; assert 'utm_source' in TRACKING_PARAM_DENYLIST; assert METRIC_POLARITY['lcp_ms'] is Polarity.LOWER_IS_BETTER; assert METRIC_POLARITY['perf_score'] is Polarity.HIGHER_IS_BETTER; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --collect-only` exits 0 (pytest installed, src layout importable, zero collection errors).
    - pyproject.toml contains a `[tool.pytest.ini_options]` table and a `[tool.ruff]` table.
    - The one-liner import check above prints `ok` (registry constants present with correct polarity values).
    - No CLI/Typer/web/measurement module created (only registry.py + __init__.py under src/perfcrawl/).
  </acceptance_criteria>
  <done>uv project scaffolded, pydantic + (w3lib if approved) installed, pytest+ruff configured, registry.py exports the two locked tables with correct polarity per D-04/D-09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Canonical URL key slice (criterion #4, D-01..D-05)</name>
  <files>src/perfcrawl/canonical.py, tests/test_canonical.py</files>
  <read_first>
    - src/perfcrawl/registry.py (the TRACKING_PARAM_DENYLIST this module imports — created in Task 2)
    - .planning/phases/01-data-model-persistence-foundation/01-RESEARCH.md (§ Code Examples canonical_key, § Pitfall 5 + 6, § Security Domain malformed-URL threat, Assumptions A2)
    - .planning/phases/01-data-model-persistence-foundation/01-CONTEXT.md (D-01..D-05)
    - .planning/phases/01-data-model-persistence-foundation/01-VALIDATION.md (the criterion #4 test rows this file must satisfy)
  </read_first>
  <behavior>
    - test_variants_collapse: canonical_key("https://Example.com/Path/?utm_source=x&b=2&a=1#frag") == canonical_key("https://example.com/Path?a=1&b=2") — scheme+host lowercased, utm dropped, query sorted, fragment dropped, trailing slash stripped, path case PRESERVED (/Path stays /Path).
    - test tracking-param drop: utm_*, gclid, fbclid removed; functional params (?id=5) kept.
    - test query sort: ?b=2&a=1 normalizes to a before b.
    - test fragment drop: #anything removed (D-05).
    - test trailing slash: "https://x/foo/" -> "https://x/foo"; root "https://x/" stays "https://x/" (D-03).
    - test default-port strip: ":80"/":443" removed (D-02); uppercase percent-hex normalized (D-02) — verify w3lib actually does this (A2), patch the wrapper if a sub-rule is missing.
    - test_no_over_merge: canonical_key("https://x/?page=2") != canonical_key("https://x/?page=3"); canonical_key("https://www.x/") != canonical_key("https://x/") — D-03/D-04 forbid stripping www and functional params (Pitfall 6).
    - test malformed input: canonical_key("not a url") and canonical_key("") return deterministically WITHOUT raising (Security Domain DoS mitigation).
  </behavior>
  <action>
    Write tests/test_canonical.py FIRST covering every behavior above, then implement src/perfcrawl/canonical.py to make them green. canonical.py exposes `def canonical_key(url: str) -> str` implementing D-01..D-05: import TRACKING_PARAM_DENYLIST from perfcrawl.registry (the ONE editable place — never inline the denylist here); if w3lib was approved in Task 1, use url_query_cleaner(url, TRACKING_PARAM_DENYLIST, remove=True, keep_fragments=False) then canonicalize_url(..., keep_fragments=False) then strip the trailing slash via urlsplit/urlunsplit except for root "/" (w3lib does NOT do D-03); if the stdlib fallback was chosen, implement the same observable transform with urllib.parse only. Preserve path case (D-02), never strip www or index.html (D-03), keep functional query params (D-04). canonical_key MUST handle malformed/non-URL input without raising (return a deterministic value). Do NOT mutate the original URL — this function only derives the key (the raw url is stored separately by the model in Plan 02, D-01).
  </action>
  <verify>
    <automated>uv run pytest tests/test_canonical.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_canonical.py::test_variants_collapse -x` exits 0.
    - `uv run pytest tests/test_canonical.py::test_no_over_merge -x` exits 0.
    - `uv run pytest tests/test_canonical.py -x -q` exits 0 (all D-02..D-05 + malformed-input cases pass).
    - Source assertion: `grep -q "from perfcrawl.registry import TRACKING_PARAM_DENYLIST" src/perfcrawl/canonical.py` succeeds (denylist not inlined — D-04 one-editable-place).
    - `canonical_key("not a url")` does not raise (run inline: `uv run python -c "from perfcrawl.canonical import canonical_key; canonical_key('not a url'); canonical_key('')"` exits 0).
  </acceptance_criteria>
  <done>canonical_key() derives a stable cross-run key per D-01..D-05; same-page variants collapse to one key, distinct pages stay distinct, malformed input is safe; the denylist lives only in registry.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → local install | Installing w3lib (new external dependency) pulls third-party code into the project. |
| untrusted URL string → canonical_key() | URLs originate from future crawl/fixtures; canonical_key parses arbitrary strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `uv add w3lib` (npm/pip-equivalent install) | mitigate | Task 1 blocking-human checkpoint verifies w3lib on pypi.org/project/w3lib before install; never auto-approved. Documented stdlib fallback if declined. |
| T-01-01 | Denial of Service | `canonical_key()` parsing untrusted/malformed URLs | mitigate | canonical_key must not raise on non-URL/empty input (returns deterministic value); test_canonical covers malformed-input case (Security Domain). |
| T-01-02 | Tampering | Pydantic/w3lib version drift | accept | Pinned ranges in pyproject.toml (`pydantic>=2.10,<3`, `w3lib>=2.3,<3`); uv.lock records exact resolved versions. Low-value local library, no network at runtime. |
</threat_model>

<verification>
- `uv run pytest -x -q` — full suite green (Plan 01 contributes test_canonical.py).
- `uv run python -c "import perfcrawl"` — package importable.
- pyproject.toml has `[tool.pytest.ini_options]` and `[tool.ruff]`.
- registry.py exports both locked tables (D-04 denylist, D-09 polarity).
</verification>

<success_criteria>
- ROADMAP success criterion #4 (canonical URL key) is satisfied and tested against fixtures.
- The project scaffold (uv, src/perfcrawl/, pytest, ruff) exists and all later plans can build on it.
- The two "one editable place" registry tables exist for Plans 02 and 03 to consume.
- w3lib install was gated through a human-verify checkpoint (or the stdlib fallback was chosen).
</success_criteria>

<output>
Create `.planning/phases/01-data-model-persistence-foundation/01-01-SUMMARY.md` when done
</output>
