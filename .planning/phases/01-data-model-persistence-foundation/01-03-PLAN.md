---
phase: 01-data-model-persistence-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/perfcrawl/delta.py
  - tests/test_delta.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Given two stored runs for the same site, RunDelta records (current/previous/deltaAbs/deltaPct/direction) are computed per page per metric against fixture data"
    - "direction is derived from the central polarity registry — lower-is-better and higher-is-better metrics classify correctly"
    - "deltaPct is None (never inf/NaN) when previous is 0 or None"
    - "A new page emits direction=new (previous=None); a disappeared page emits direction=removed (current=None) and is never silently dropped"
    - "A metric present on only one side emits direction=not_comparable"
    - "unchanged means literal equality (no noise band — that is Phase 6)"
  artifacts:
    - path: "src/perfcrawl/delta.py"
      provides: "compute_deltas(current_run, previous_run) -> list[RunDelta] + RunDelta model + classify/safe_pct helpers"
      contains: "def compute_deltas"
      exports: ["compute_deltas", "RunDelta"]
    - path: "tests/test_delta.py"
      provides: "polarity-driven direction, deltaPct zero guard, new/removed/not_comparable, unchanged literal-equality assertions"
      contains: "test_direction_by_polarity"
  key_links:
    - from: "src/perfcrawl/delta.py"
      to: "src/perfcrawl/registry.py"
      via: "reads METRIC_POLARITY to derive direction (D-09)"
      pattern: "from perfcrawl.registry import METRIC_POLARITY"
    - from: "src/perfcrawl/delta.py"
      to: "src/perfcrawl/models.py"
      via: "consumes RunRecord/PageResult, returns RunDelta using DirectionStatus"
      pattern: "DirectionStatus"
---

<objective>
Deliver the final capability slice: compute RunDelta records (current / previous / deltaAbs / deltaPct / direction) per page per metric from two stored RunRecords, with direction derived from the central polarity registry (D-09), raw direction only (D-12), edge cases routed through the DirectionStatus enum (D-11), and deltaPct guarded against previous==0 (D-10). Verified against the two-run fixture pair (success criterion #2).

Purpose: Closes the last of the four Phase 1 success criteria. Phase 6 will add the noise-band threshold ON TOP of this raw direction — Phase 1 must NOT pre-empt it (D-12). The polarity table lives in registry.py (Plan 01) so later phases extend it without touching this call site.
Output: delta.py (RunDelta model + compute_deltas) and green test_delta.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-model-persistence-foundation/01-CONTEXT.md
@.planning/phases/01-data-model-persistence-foundation/01-RESEARCH.md
@.planning/phases/01-data-model-persistence-foundation/01-VALIDATION.md

# Plan 01 (registry/polarity) and Plan 02 (models + fixtures) are consumed directly:
@.planning/phases/01-data-model-persistence-foundation/01-01-SUMMARY.md
@.planning/phases/01-data-model-persistence-foundation/01-02-SUMMARY.md
@src/perfcrawl/registry.py
@src/perfcrawl/models.py

<interfaces>
<!-- Contracts this plan CONSUMES (no codebase exploration needed). -->

From src/perfcrawl/registry.py (Plan 01):
  class Polarity(StrEnum): LOWER_IS_BETTER = "lower"; HIGHER_IS_BETTER = "higher"
  METRIC_POLARITY: dict[str, Polarity]   # field name -> polarity (the ONE editable place, D-09)

From src/perfcrawl/models.py (Plan 02):
  class RunRecord(BaseModel): pages: list[PageResult]; ...
  class PageResult(BaseModel): url: str; url_key: str; perf_score: float|None; lcp_ms: MetricSample|None; ...
  class DirectionStatus(StrEnum): IMPROVEMENT/REGRESSION/UNCHANGED/NEW/REMOVED/NOT_COMPARABLE   # D-11

This plan CREATES (delta.py):
  class RunDelta(BaseModel): url_key: str; metric: str; current: float|None; previous: float|None; delta_abs: float|None; delta_pct: float|None; direction: DirectionStatus
  def compute_deltas(current_run: RunRecord, previous_run: RunRecord) -> list[RunDelta]   # flat list keyed by (url_key, metric)

Recommended classify/safe_pct logic (RESEARCH.md Pattern 5 — RAW direction only, no noise band):
  classify(metric, current, previous): new if previous is None; removed if current is None; not_comparable if metric not in METRIC_POLARITY; unchanged if current==previous; else improvement/regression by polarity.
  safe_pct(current, previous): None when previous in (None, 0) or current is None; else (current-previous)/previous*100.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Polarity-driven RunDelta engine (criterion #2, D-09/D-10/D-11/D-12)</name>
  <files>src/perfcrawl/delta.py, tests/test_delta.py</files>
  <read_first>
    - .planning/phases/01-data-model-persistence-foundation/01-RESEARCH.md (§ Pattern 5 polarity-driven engine, § Pitfall 3 deltaPct guard, § Pitfall 4 disappeared pages, § Open Questions Q2 flat-list shape)
    - src/perfcrawl/registry.py (METRIC_POLARITY / Polarity — derives direction, D-09)
    - src/perfcrawl/models.py (RunRecord, PageResult, DirectionStatus — consumed/returned)
    - tests/conftest.py (the two-run delta-pair fixture from Plan 02)
    - .planning/phases/01-data-model-persistence-foundation/01-CONTEXT.md (D-09, D-10, D-11, D-12)
    - .planning/phases/01-data-model-persistence-foundation/01-VALIDATION.md (all criterion #2 test rows)
  </read_first>
  <behavior>
    - test_direction_by_polarity: for a lower-is-better metric (e.g. lcp_ms / ttfb_ms), current < previous => direction=improvement and current > previous => regression; for a higher-is-better metric (e.g. perf_score), current > previous => improvement and current < previous => regression (D-09, derived from METRIC_POLARITY — NOT hardcoded).
    - test_deltapct_zero_guard: when previous == 0, delta_pct is None (no inf/NaN, no ZeroDivisionError); delta_abs still computed (D-10).
    - test_edge_status_enum: a page only in current => direction=new, previous=None; a page only in previous => direction=removed, current=None AND it IS present in the output (never silently dropped, D-11); a metric present on only one side => direction=not_comparable.
    - test_unchanged_is_literal: current == previous => direction=unchanged (literal equality; NO noise band — D-12). A tiny non-equal change (e.g. 100.0 vs 100.1) on a lower-is-better metric => improvement/regression, NOT unchanged (proving the noise band is NOT pre-empted).
    - test_flat_list_shape: compute_deltas returns a flat list[RunDelta] keyed by (url_key, metric) over the UNION of pages and the UNION of metrics (Open Q2 recommendation).
  </behavior>
  <action>
    Write tests/test_delta.py FIRST using the two-run fixture pair (extend conftest.py if the pair needs the specific cases: an improved metric, a regressed metric, an unchanged metric, a previous==0 metric, a NEW page, a REMOVED page, and a metric present on only one side). Then implement src/perfcrawl/delta.py. Define RunDelta(BaseModel) with fields url_key: str, metric: str, current: float | None, previous: float | None, delta_abs: float | None, delta_pct: float | None, direction: DirectionStatus (import DirectionStatus from perfcrawl.models — do NOT redefine it). Implement a classify(metric, current, previous) -> DirectionStatus helper deriving direction from METRIC_POLARITY (import from perfcrawl.registry — NEVER hardcode lower/higher-is-better at the call site, D-09): previous is None => NEW; current is None => REMOVED (D-11, emitted not dropped); metric not in METRIC_POLARITY => NOT_COMPARABLE (schema drift, D-11); current == previous => UNCHANGED (literal equality, D-12 — no noise band); else IMPROVEMENT/REGRESSION by polarity. Implement safe_pct(current, previous) -> float | None returning None when previous in (None, 0) or current is None (guard inf/NaN, D-10). Implement compute_deltas(current_run, previous_run) -> list[RunDelta]: build url_key->PageResult maps for both runs; iterate the UNION of url_keys (so removed pages are emitted, D-11/Pitfall 4); for each page iterate the UNION of comparable metric field names (the keys of METRIC_POLARITY, extracting the scalar to compare — for MetricSample fields compare the median); emit one RunDelta per (url_key, metric) with current, previous, delta_abs (current-previous when both present else None), delta_pct via safe_pct, and direction via classify. Return a FLAT list (Open Q2). Compute RAW direction ONLY — do NOT implement any threshold/noise band (that is Phase 6, D-12).
  </action>
  <verify>
    <automated>uv run pytest tests/test_delta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_delta.py::test_direction_by_polarity -x` exits 0 (D-09 polarity-derived).
    - `uv run pytest tests/test_delta.py::test_deltapct_zero_guard -x` exits 0 (D-10, previous==0 => delta_pct is None).
    - `uv run pytest tests/test_delta.py::test_edge_status_enum -x` exits 0 (new/removed/not_comparable; removed emitted, D-11).
    - `uv run pytest tests/test_delta.py::test_unchanged_is_literal -x` exits 0 (D-12 literal equality, no noise band).
    - Source assertion: `grep -q "from perfcrawl.registry import METRIC_POLARITY" src/perfcrawl/delta.py` succeeds (polarity NOT hardcoded — D-09 one-editable-place).
    - Source assertion: delta.py does NOT redefine DirectionStatus — `grep -q "from perfcrawl.models import" src/perfcrawl/delta.py` succeeds and `grep -q "class DirectionStatus" src/perfcrawl/delta.py` FAILS.
    - Source assertion: no threshold/noise-band literal in delta.py — `grep -Eiq "noise|threshold|tolerance|noise_band" src/perfcrawl/delta.py` FAILS (D-12 not pre-empted).
  </acceptance_criteria>
  <done>compute_deltas emits a flat list[RunDelta] per (page, metric) over the union of both runs; direction is derived from the central polarity registry; deltaPct is guarded against previous==0; new/removed/not_comparable are emitted (removed never dropped); unchanged is literal equality with no noise band.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two RunRecords → compute_deltas | Pure in-memory computation over already-validated typed models; no I/O, no network, no untrusted external input beyond the models. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-D | Denial of Service | compute_deltas on malformed numerics (previous==0, None) | mitigate | safe_pct guards division (returns None, never inf/NaN/ZeroDivisionError); classify handles None on either side — covered by test_deltapct_zero_guard + test_edge_status_enum. |
| T-01-N | Tampering | direction logic correctness (regression mislabeled as improvement) | mitigate | direction derived from the single METRIC_POLARITY registry, never hardcoded; test_direction_by_polarity asserts both polarities. No call-site polarity logic (grep-asserted). |
</threat_model>

<verification>
- `uv run pytest -x -q` — full Phase 1 suite green (Plan 03 adds test_delta.py; all four criteria now covered).
- delta.py reads polarity only from registry.py and reuses DirectionStatus from models.py.
- No noise-band/threshold logic present (Phase 6 boundary respected).
</verification>

<success_criteria>
- ROADMAP success criterion #2 (RunDelta per page per metric on fixtures) satisfied and tested.
- All four Phase 1 success criteria are now green across Plans 01-03; HIST-01 covered by Plan 02.
- Polarity lives in one editable place; direction is derived; deltaPct is guarded; removed pages are emitted; unchanged is literal equality (Phase 6 noise band not pre-empted).
</success_criteria>

<output>
Create `.planning/phases/01-data-model-persistence-foundation/01-03-SUMMARY.md` when done
</output>
